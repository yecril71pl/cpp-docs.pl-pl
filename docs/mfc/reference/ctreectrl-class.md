---
title: Klasa CTreeCtrl
ms.date: 11/04/2016
f1_keywords:
- CTreeCtrl
- AFXCMN/CTreeCtrl
- AFXCMN/CTreeCtrl::CTreeCtrl
- AFXCMN/CTreeCtrl::Create
- AFXCMN/CTreeCtrl::CreateDragImage
- AFXCMN/CTreeCtrl::CreateEx
- AFXCMN/CTreeCtrl::DeleteAllItems
- AFXCMN/CTreeCtrl::DeleteItem
- AFXCMN/CTreeCtrl::EditLabel
- AFXCMN/CTreeCtrl::EndEditLabelNow
- AFXCMN/CTreeCtrl::EnsureVisible
- AFXCMN/CTreeCtrl::Expand
- AFXCMN/CTreeCtrl::GetBkColor
- AFXCMN/CTreeCtrl::GetCheck
- AFXCMN/CTreeCtrl::GetChildItem
- AFXCMN/CTreeCtrl::GetCount
- AFXCMN/CTreeCtrl::GetDropHilightItem
- AFXCMN/CTreeCtrl::GetEditControl
- AFXCMN/CTreeCtrl::GetExtendedStyle
- AFXCMN/CTreeCtrl::GetFirstVisibleItem
- AFXCMN/CTreeCtrl::GetImageList
- AFXCMN/CTreeCtrl::GetIndent
- AFXCMN/CTreeCtrl::GetInsertMarkColor
- AFXCMN/CTreeCtrl::GetItem
- AFXCMN/CTreeCtrl::GetItemData
- AFXCMN/CTreeCtrl::GetItemExpandedImageIndex
- AFXCMN/CTreeCtrl::GetItemHeight
- AFXCMN/CTreeCtrl::GetItemImage
- AFXCMN/CTreeCtrl::GetItemPartRect
- AFXCMN/CTreeCtrl::GetItemRect
- AFXCMN/CTreeCtrl::GetItemState
- AFXCMN/CTreeCtrl::GetItemStateEx
- AFXCMN/CTreeCtrl::GetItemText
- AFXCMN/CTreeCtrl::GetLastVisibleItem
- AFXCMN/CTreeCtrl::GetLineColor
- AFXCMN/CTreeCtrl::GetNextItem
- AFXCMN/CTreeCtrl::GetNextSiblingItem
- AFXCMN/CTreeCtrl::GetNextVisibleItem
- AFXCMN/CTreeCtrl::GetParentItem
- AFXCMN/CTreeCtrl::GetPrevSiblingItem
- AFXCMN/CTreeCtrl::GetPrevVisibleItem
- AFXCMN/CTreeCtrl::GetRootItem
- AFXCMN/CTreeCtrl::GetScrollTime
- AFXCMN/CTreeCtrl::GetSelectedCount
- AFXCMN/CTreeCtrl::GetSelectedItem
- AFXCMN/CTreeCtrl::GetTextColor
- AFXCMN/CTreeCtrl::GetToolTips
- AFXCMN/CTreeCtrl::GetVisibleCount
- AFXCMN/CTreeCtrl::HitTest
- AFXCMN/CTreeCtrl::InsertItem
- AFXCMN/CTreeCtrl::ItemHasChildren
- AFXCMN/CTreeCtrl::MapAccIdToItem
- AFXCMN/CTreeCtrl::MapItemToAccID
- AFXCMN/CTreeCtrl::Select
- AFXCMN/CTreeCtrl::SelectDropTarget
- AFXCMN/CTreeCtrl::SelectItem
- AFXCMN/CTreeCtrl::SelectSetFirstVisible
- AFXCMN/CTreeCtrl::SetAutoscrollInfo
- AFXCMN/CTreeCtrl::SetBkColor
- AFXCMN/CTreeCtrl::SetCheck
- AFXCMN/CTreeCtrl::SetExtendedStyle
- AFXCMN/CTreeCtrl::SetImageList
- AFXCMN/CTreeCtrl::SetIndent
- AFXCMN/CTreeCtrl::SetInsertMark
- AFXCMN/CTreeCtrl::SetInsertMarkColor
- AFXCMN/CTreeCtrl::SetItem
- AFXCMN/CTreeCtrl::SetItemData
- AFXCMN/CTreeCtrl::SetItemExpandedImageIndex
- AFXCMN/CTreeCtrl::SetItemHeight
- AFXCMN/CTreeCtrl::SetItemImage
- AFXCMN/CTreeCtrl::SetItemState
- AFXCMN/CTreeCtrl::SetItemStateEx
- AFXCMN/CTreeCtrl::SetItemText
- AFXCMN/CTreeCtrl::SetLineColor
- AFXCMN/CTreeCtrl::SetScrollTime
- AFXCMN/CTreeCtrl::SetTextColor
- AFXCMN/CTreeCtrl::SetToolTips
- AFXCMN/CTreeCtrl::ShowInfoTip
- AFXCMN/CTreeCtrl::SortChildren
- AFXCMN/CTreeCtrl::SortChildrenCB
helpviewer_keywords:
- CTreeCtrl [MFC], CTreeCtrl
- CTreeCtrl [MFC], Create
- CTreeCtrl [MFC], CreateDragImage
- CTreeCtrl [MFC], CreateEx
- CTreeCtrl [MFC], DeleteAllItems
- CTreeCtrl [MFC], DeleteItem
- CTreeCtrl [MFC], EditLabel
- CTreeCtrl [MFC], EndEditLabelNow
- CTreeCtrl [MFC], EnsureVisible
- CTreeCtrl [MFC], Expand
- CTreeCtrl [MFC], GetBkColor
- CTreeCtrl [MFC], GetCheck
- CTreeCtrl [MFC], GetChildItem
- CTreeCtrl [MFC], GetCount
- CTreeCtrl [MFC], GetDropHilightItem
- CTreeCtrl [MFC], GetEditControl
- CTreeCtrl [MFC], GetExtendedStyle
- CTreeCtrl [MFC], GetFirstVisibleItem
- CTreeCtrl [MFC], GetImageList
- CTreeCtrl [MFC], GetIndent
- CTreeCtrl [MFC], GetInsertMarkColor
- CTreeCtrl [MFC], GetItem
- CTreeCtrl [MFC], GetItemData
- CTreeCtrl [MFC], GetItemExpandedImageIndex
- CTreeCtrl [MFC], GetItemHeight
- CTreeCtrl [MFC], GetItemImage
- CTreeCtrl [MFC], GetItemPartRect
- CTreeCtrl [MFC], GetItemRect
- CTreeCtrl [MFC], GetItemState
- CTreeCtrl [MFC], GetItemStateEx
- CTreeCtrl [MFC], GetItemText
- CTreeCtrl [MFC], GetLastVisibleItem
- CTreeCtrl [MFC], GetLineColor
- CTreeCtrl [MFC], GetNextItem
- CTreeCtrl [MFC], GetNextSiblingItem
- CTreeCtrl [MFC], GetNextVisibleItem
- CTreeCtrl [MFC], GetParentItem
- CTreeCtrl [MFC], GetPrevSiblingItem
- CTreeCtrl [MFC], GetPrevVisibleItem
- CTreeCtrl [MFC], GetRootItem
- CTreeCtrl [MFC], GetScrollTime
- CTreeCtrl [MFC], GetSelectedCount
- CTreeCtrl [MFC], GetSelectedItem
- CTreeCtrl [MFC], GetTextColor
- CTreeCtrl [MFC], GetToolTips
- CTreeCtrl [MFC], GetVisibleCount
- CTreeCtrl [MFC], HitTest
- CTreeCtrl [MFC], InsertItem
- CTreeCtrl [MFC], ItemHasChildren
- CTreeCtrl [MFC], MapAccIdToItem
- CTreeCtrl [MFC], MapItemToAccID
- CTreeCtrl [MFC], Select
- CTreeCtrl [MFC], SelectDropTarget
- CTreeCtrl [MFC], SelectItem
- CTreeCtrl [MFC], SelectSetFirstVisible
- CTreeCtrl [MFC], SetAutoscrollInfo
- CTreeCtrl [MFC], SetBkColor
- CTreeCtrl [MFC], SetCheck
- CTreeCtrl [MFC], SetExtendedStyle
- CTreeCtrl [MFC], SetImageList
- CTreeCtrl [MFC], SetIndent
- CTreeCtrl [MFC], SetInsertMark
- CTreeCtrl [MFC], SetInsertMarkColor
- CTreeCtrl [MFC], SetItem
- CTreeCtrl [MFC], SetItemData
- CTreeCtrl [MFC], SetItemExpandedImageIndex
- CTreeCtrl [MFC], SetItemHeight
- CTreeCtrl [MFC], SetItemImage
- CTreeCtrl [MFC], SetItemState
- CTreeCtrl [MFC], SetItemStateEx
- CTreeCtrl [MFC], SetItemText
- CTreeCtrl [MFC], SetLineColor
- CTreeCtrl [MFC], SetScrollTime
- CTreeCtrl [MFC], SetTextColor
- CTreeCtrl [MFC], SetToolTips
- CTreeCtrl [MFC], ShowInfoTip
- CTreeCtrl [MFC], SortChildren
- CTreeCtrl [MFC], SortChildrenCB
ms.assetid: 96e20031-6161-4143-8c12-8d1816c66d90
ms.openlocfilehash: 6fe7b133fa01285f85dc28dc8d8ca01e027f4a5c
ms.sourcegitcommit: c123cc76bb2b6c5cde6f4c425ece420ac733bf70
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 04/14/2020
ms.locfileid: "81373304"
---
# <a name="ctreectrl-class"></a>Klasa CTreeCtrl

Udostępnia funkcje formantu wspólnego widoku drzewa systemu Windows.

## <a name="syntax"></a>Składnia

```
class CTreeCtrl : public CWnd
```

## <a name="members"></a>Elementy członkowskie

### <a name="public-constructors"></a>Konstruktory publiczne

|Nazwa|Opis|
|----------|-----------------|
|[CTreeCtrl::CTreeCtrl](#ctreectrl)|Konstruuje `CTreeCtrl` obiekt.|

### <a name="public-methods"></a>Metody publiczne

|Nazwa|Opis|
|----------|-----------------|
|[CTreeCtrl::Utwórz](#create)|Tworzy formant widoku drzewa i `CTreeCtrl` dołącza go do obiektu.|
|[CTreeCtrl::CreateDragImage](#createdragimage)|Tworzy przeciągającą mapę bitową dla określonego elementu widoku drzewa.|
|[CTreeCtrl::CreateEx](#createex)|Tworzy formant drzewa z określonymi stylami rozszerzonymi `CTreeCtrl` systemu Windows i dołącza go do obiektu.|
|[CTreeCtrl::DeleteWszystkie elementy](#deleteallitems)|Usuwa wszystkie elementy w formancie widoku drzewa.|
|[CTreeCtrl::DeleteItem](#deleteitem)|Usuwa nowy element w formancie widoku drzewa.|
|[CTreeCtrl::EditLabel](#editlabel)|Edytuje określony element widoku drzewa w miejscu.|
|[CTreeCtrl::EndEditLabelNow](#endeditlabelnow)|Anuluje operację edycji na etykiecie elementu widoku drzewa w bieżącym formancie widoku drzewa.|
|[CTreeCtrl::EnsureVisible](#ensurevisible)|Zapewnia, że element widoku drzewa jest widoczny w jego formancie widoku drzewa.|
|[CTreeCtrl::Rozwiń](#expand)|Rozwija lub zwija elementy podrzędne określonego elementu widoku drzewa.|
|[CTreeCtrl::GetBkColor](#getbkcolor)|Pobiera bieżący kolor tła formantu.|
|[CTreeCtrl::GetCheck](#getcheck)|Pobiera stan kontroli elementu formantu drzewa.|
|[CTreeCtrl::GetChildItem](#getchilditem)|Pobiera element podrzędny określonego elementu widoku drzewa.|
|[CTreeCtrl::GetCount](#getcount)|Pobiera liczbę elementów drzewa skojarzonych z formantem widoku drzewa.|
|[CTreeCtrl::GetDropHilightItem](#getdrophilightitem)|Pobiera obiekt docelowy operacji przeciągania i upuszczania.|
|[CTreeCtrl::GetEditControl](#geteditcontrol)|Pobiera uchwyt formantu edycji używanego do edytowania określonego elementu widoku drzewa.|
|[CTreeCtrl::GetExtendedStyle](#getextendedstyle)|Pobiera rozszerzone style, które jest używany bieżącego formantu widoku drzewa.|
|[CTreeCtrl::GetFirstVisibleItem](#getfirstvisibleitem)|Pobiera pierwszy widoczny element określonego elementu widoku drzewa.|
|[CTreeCtrl::Lista GetImage](#getimagelist)|Pobiera dojście listy obrazów skojarzonych z formantem widoku drzewa.|
|[CTreeCtrl::GetIndent](#getindent)|Pobiera przesunięcie (w pikselach) elementu widoku drzewa z jego elementu nadrzędnego.|
|[CTreeCtrl::GetInsertMarkColor](#getinsertmarkcolor)|Pobiera kolor używany do rysowania znacznika wstawiania dla widoku drzewa.|
|[CTreeCtrl::GetItem](#getitem)|Pobiera atrybuty określonego elementu widoku drzewa.|
|[CTreeCtrl::GetItemData](#getitemdata)|Zwraca wartość specyficzną dla aplikacji skojarzoną z towarem.|
|[CTreeCtrl::GetItemRozwińImdeks](#getitemexpandedimageindex)|Pobiera indeks obrazu do wyświetlenia, gdy określony element bieżącego formantu widoku drzewa jest w stanie rozwiniętym.|
|[CTreeCtrl::GetItemHeight](#getitemheight)|Pobiera bieżącą wysokość elementów widoku drzewa.|
|[CTreeCtrl::GetItemImage](#getitemimage)|Pobiera obrazy skojarzone z elementem.|
|[CTreeCtrl::GetItemPartrect](#getitempartrect)|Pobiera prostokąt ograniczający dla określonej części określonego elementu w bieżącym formancie widoku drzewa.|
|[CTreeCtrl::GetItemRect](#getitemrect)|Pobiera prostokąt ograniczający elementu widoku drzewa.|
|[CTreeCtrl::GetItemState](#getitemstate)|Zwraca stan towaru.|
|[CTreeCtrl::GetItemStateEx](#getitemstateex)|Pobiera rozszerzony stan określonego elementu w bieżącym formancie widoku drzewa.|
|[CTreeCtrl::GetItemText](#getitemtext)|Zwraca tekst elementu.|
|[CTreeCtrl::GetLastVisibleItem](#getlastvisibleitem)|Pobiera ostatni rozwinięty element w bieżącym formancie widoku drzewa.|
|[CTreeCtrl::GetLineColor](#getlinecolor)|Pobiera bieżący kolor linii dla formantu widoku drzewa.|
|[CTreeCtrl::GetNextItem](#getnextitem)|Pobiera następny element widoku drzewa, który pasuje do określonej relacji.|
|[CTreeCtrl::GetNextSiblingItem](#getnextsiblingitem)|Pobiera następną część równorzędna określonego elementu widoku drzewa.|
|[CTreeCtrl::GetNextVisibleItem](#getnextvisibleitem)|Pobiera następny widoczny element określonego elementu widoku drzewa.|
|[CTreeCtrl::GetParentItem](#getparentitem)|Pobiera element nadrzędny określonego elementu widoku drzewa.|
|[CTreeCtrl::GetPrevSiblingItem](#getprevsiblingitem)|Pobiera poprzedni element równorzędny określonego elementu widoku drzewa.|
|[CTreeCtrl::GetPrevVisibleItem](#getprevvisibleitem)|Pobiera poprzedni widoczny element określonego elementu widoku drzewa.|
|[CTreeCtrl::GetRootItem](#getrootitem)|Pobiera katalog główny określonego elementu widoku drzewa.|
|[CTreeCtrl::GetScrollTime](#getscrolltime)|Pobiera maksymalny czas przewijania dla formantu widoku drzewa.|
|[CTreeCtrl::GetSelectedCount](#getselectedcount)|Pobiera liczbę wybranych elementów w bieżącym formancie widoku drzewa.|
|[CTreeCtrl::GetSelectedItem](#getselecteditem)|Pobiera aktualnie zaznaczony element widoku drzewa.|
|[CTreeCtrl::GetTextColor](#gettextcolor)|Pobiera bieżący kolor tekstu formantu.|
|[CTreeCtrl::Poradek GetToolTips](#gettooltips)|Pobiera dojście do podrzędnego formantu etykietki narzędzi używanej przez formant widoku drzewa.|
|[CTreeCtrl::GetVisibleCount](#getvisiblecount)|Pobiera liczbę widocznych elementów drzewa skojarzonych z formantem widoku drzewa.|
|[CTreeCtrl::HitTest](#hittest)|Zwraca bieżącą pozycję kursora powiązanego `CTreeCtrl` z obiektem.|
|[CTreeCtrl::InsertItem](#insertitem)|Wstawia nowy element w formancie widoku drzewa.|
|[CTreeCtrl::ItemHasChildren](#itemhaschildren)|Zwraca wartość niezerowa, jeśli określony element ma elementy podrzędne.|
|[CTreeCtrl::MapAccIdToItem](#mapaccidtoitem)|Mapuje określony identyfikator ułatwień dostępu do dojścia do elementu widoku drzewa w bieżącym formancie widoku drzewa.|
|[CTreeCtrl::MapItemToAccID](#mapitemtoaccid)|Mapuje określony dojście do elementu widoku drzewa w bieżącym formancie widoku drzewa do identyfikatora ułatwień dostępu.|
|[CTreeCtrl::Wybierz](#select)|Zaznacza, przewija do widoku lub ponownie rysuje określony element widoku drzewa.|
|[CTreeCtrl::SelectDropTarget](#selectdroptarget)|Ponownie rysuje element drzewa jako cel operacji przeciągania i upuszczania.|
|[CTreeCtrl::SelectItem](#selectitem)|Wybiera określony element widoku drzewa.|
|[CTreeCtrl::SelectSetFirstVisible](#selectsetfirstvisible)|Wybiera określony element widoku drzewa jako pierwszy widoczny element.|
|[CTreeCtrl::SetAutoscrollInfo](#setautoscrollinfo)|Ustawia szybkość automatycznego podglądu bieżącej kontrolki widoku drzewa.|
|[CTreeCtrl::SetBkColor](#setbkcolor)|Ustawia kolor tła formantu.|
|[CTreeCtrl::SetCheck](#setcheck)|Ustawia stan kontroli elementu sterującego drzewa.|
|[CTreeCtrl::SetExtendedStyle](#setextendedstyle)|Ustawia rozszerzone style dla bieżącego formantu widoku drzewa.|
|[CTreeCtrl::SetImageList](#setimagelist)|Ustawia uchwyt listy obrazów skojarzonych z formantem widoku drzewa.|
|[CTreeCtrl::SetIndent](#setindent)|Ustawia przesunięcie (w pikselach) elementu widoku drzewa z jego elementu nadrzędnego.|
|[CTreeCtrl::SetInsertMark](#setinsertmark)|Ustawia znacznik wstawiania w formancie widoku drzewa.|
|[CTreeCtrl::SetInsertMarkColor](#setinsertmarkcolor)|Ustawia kolor używany do rysowania znacznika wstawiania dla widoku drzewa.|
|[CTreeCtrl::SetItem](#setitem)|Ustawia atrybuty określonego elementu widoku drzewa.|
|[CTreeCtrl::SetItemData](#setitemdata)|Ustawia wartość specyficzną dla aplikacji skojarzoną z elementem.|
|[CTreeCtrl::SetItemRozwińImedindex](#setitemexpandedimageindex)|Ustawia indeks obrazu do wyświetlenia, gdy określony element bieżącego formantu widoku drzewa jest w stanie rozwiniętym.|
|[CTreeCtrl::SetItemHeight](#setitemheight)|Ustawia wysokość elementów widoku drzewa.|
|[CTreeCtrl::SetItemImage](#setitemimage)|Kojarzy obrazy z elementem.|
|[CTreeCtrl::Stan zestawu](#setitemstate)|Ustawia stan elementu.|
|[CTreeCtrl::SetItemStateEx](#setitemstateex)|Ustawia rozszerzony stan określonego elementu w bieżącym formancie widoku drzewa.|
|[CTreeCtrl::SetItemText](#setitemtext)|Ustawia tekst elementu.|
|[CTreeCtrl::SetLineColor](#setlinecolor)|Ustawia bieżący kolor linii dla formantu widoku drzewa.|
|[CTreeCtrl::SetScrollTime](#setscrolltime)|Ustawia maksymalny czas przewijania dla formantu widoku drzewa.|
|[CTreeCtrl::SetTextColor](#settextcolor)|Ustawia kolor tekstu formantu.|
|[CTreeCtrl::SetToolTips](#settooltips)|Ustawia podrzędną etykietkę narzędzia formantu widoku drzewa.|
|[CTreeCtrl::PokażTip](#showinfotip)|Wyświetla informacje dla określonego elementu w bieżącym formancie widoku drzewa.|
|[CTreeCtrl::SortChildren](#sortchildren)|Sortuje element podrzędny danego elementu nadrzędnego.|
|[CTreeCtrl::SortChildrenCB](#sortchildrencb)|Sortuje element podrzędny danego elementu nadrzędnego przy użyciu funkcji sortowania zdefiniowanej przez aplikację.|

## <a name="remarks"></a>Uwagi

"Formant widoku drzewa" to okno, w które wyświetlana jest hierarchiczna lista elementów, takich jak nagłówki w dokumencie, wpisy w indeksie lub pliki i katalogi na dysku. Każdy element składa się z etykiety i opcjonalnego obrazu bitmapowego, a każdy element może mieć z nim skojarzoną listę podelementów. Klikając element, użytkownik może rozwinąć i zwinąć skojarzoną listę podelementów.

Ten formant (i `CTreeCtrl` dlatego klasa) jest dostępny tylko dla programów działających w systemach Windows 98 i Windows NT w wersji 4 i nowszych.

Aby uzyskać więcej `CTreeCtrl`informacji na temat korzystania z programu , zobacz:

- [Formanty](../../mfc/controls-mfc.md)

- [Korzystanie z CTreeCtrl](../../mfc/using-ctreectrl.md)

- [Odwołanie do kontroli widoku drzewa](/windows/win32/Controls/tree-view-control-reference) w panelu Windows SDK.

## <a name="inheritance-hierarchy"></a>Hierarchia dziedziczenia

[Cobject](../../mfc/reference/cobject-class.md)

[Ccmdtarget](../../mfc/reference/ccmdtarget-class.md)

[Cwnd](../../mfc/reference/cwnd-class.md)

`CTreeCtrl`

## <a name="requirements"></a>Wymagania

**Nagłówek:** afxcmn.h

## <a name="ctreectrlcreate"></a><a name="create"></a>CTreeCtrl::Utwórz

Jeśli formant drzewa zostanie określony w szablonie okna dialogowego lub jeśli używasz [CTreeView,](../../mfc/reference/ctreeview-class.md)formant drzewa jest tworzony automatycznie po utworzeniu okna dialogowego lub widoku.

```
virtual BOOL Create(
    DWORD dwStyle,
    const RECT& rect,
    CWnd* pParentWnd,
    UINT nID);
```

### <a name="parameters"></a>Parametry

*Dwstyle*<br/>
Określa styl formantu widoku drzewa. Zastosuj style okien opisane w [programie CreateWindow](/windows/win32/api/winuser/nf-winuser-createwindoww)oraz dowolną kombinację [stylów sterowania widokiem drzewa,](/windows/win32/Controls/tree-view-control-window-styles) zgodnie z opisem w zestaw windows SDK.

*Rect*<br/>
Określa rozmiar i położenie formantu widoku drzewa. Może to być obiekt [CRect](../../atl-mfc-shared/reference/crect-class.md) lub [struktura RECT.](/previous-versions/dd162897\(v=vs.85\))

*pParentWnd*<br/>
Określa okno nadrzędne formantu widoku `CDialog`drzewa, zwykle . Nie może być null.

*Nid*<br/>
Określa identyfikator formantu widoku drzewa.

### <a name="return-value"></a>Wartość zwracana

Nonzero, jeśli inicjowanie zakończyło się pomyślnie; w przeciwnym razie 0.

### <a name="remarks"></a>Uwagi

Jeśli chcesz utworzyć formant drzewa jako okno podrzędne innego `Create` okna, użyj funkcji elementu członkowskiego. Jeśli formant drzewa `Create`zostanie utworzony przy użyciu , należy przekazać go WS_VISIBLE, oprócz innych stylów widoku drzewa.

Konstruujesz `CTreeCtrl` w dwóch krokach. Najpierw wywołać konstruktora, a następnie wywołać `Create`, który `CTreeCtrl` tworzy formant widoku drzewa i dołącza go do obiektu.

Aby utworzyć formant drzewa z rozszerzonymi stylami `Create`okien, [zamiast](#createex) .

### <a name="example"></a>Przykład

[!code-cpp[NVC_MFC_CTreeCtrl#1](../../mfc/reference/codesnippet/cpp/ctreectrl-class_1.cpp)]

## <a name="ctreectrlcreateex"></a><a name="createex"></a>CTreeCtrl::CreateEx

Wywołanie tej funkcji, aby utworzyć formant (okno `CTreeCtrl` podrzędne) i skojarzyć go z obiektem.

```
virtual BOOL CreateEx(
    DWORD dwExStyle,
    DWORD dwStyle,
    const RECT& rect,
    CWnd* pParentWnd,
    UINT nID);
```

### <a name="parameters"></a>Parametry

*Dwexstyle*<br/>
Określa rozszerzony styl tworzonego formantu. Aby uzyskać listę rozszerzonych stylów systemu Windows, zobacz parametr *dwExStyle* dla [createwindowex](/windows/win32/api/winuser/nf-winuser-createwindowexw) w zestawie Windows SDK.

*Dwstyle*<br/>
Określa styl formantu widoku drzewa. Zastosuj style okien opisane w [programie CreateWindow](/windows/win32/api/winuser/nf-winuser-createwindoww)oraz dowolną kombinację [stylów sterowania widokiem drzewa,](/windows/win32/Controls/tree-view-control-window-styles) zgodnie z opisem w zestaw windows SDK.

*Rect*<br/>
Odwołanie do struktury [RECT](/previous-versions/dd162897\(v=vs.85\)) opisujące rozmiar i położenie okna, które ma zostać utworzone, we współrzędnych klienta *pParentWnd*.

*pParentWnd*<br/>
Wskaźnik do okna, które jest nadrzędnym formantu.

*Nid*<br/>
Identyfikator okna podrzędnego formantu.

### <a name="return-value"></a>Wartość zwracana

Nonzero jeśli się powiedzie inaczej 0.

### <a name="remarks"></a>Uwagi

Użyj `CreateEx` zamiast [Create,](#create) aby zastosować rozszerzone style systemu Windows, określone przez przedmową styl rozszerzony systemu Windows **WS_EX_**.

## <a name="ctreectrlcreatedragimage"></a><a name="createdragimage"></a>CTreeCtrl::CreateDragImage

Wywołanie tej funkcji w celu utworzenia przeciągającej mapy bitowej dla danego elementu w formancie widoku drzewa, utworzenia listy obrazów dla mapy bitowej i dodania mapy bitowej do listy obrazów.

```
CImageList* CreateDragImage(HTREEITEM hItem);
```

### <a name="parameters"></a>Parametry

*hItem (własówce)*<br/>
Uchwyt elementu drzewa, który ma zostać przeciągnięty.

### <a name="return-value"></a>Wartość zwracana

Wskaźnik do listy obrazów, do której dodano przeciągającą mapę bitową, jeśli zakończy się pomyślnie; w przeciwnym razie NULL.

### <a name="remarks"></a>Uwagi

Aplikacja używa funkcji listy obrazów do wyświetlania obrazu podczas przeciągania elementu.

Obiekt `CImageList` jest trwały i należy go usunąć po zakończeniu. Przykład:

[!code-cpp[NVC_MFC_CTreeCtrl#2](../../mfc/reference/codesnippet/cpp/ctreectrl-class_2.cpp)]

## <a name="ctreectrlctreectrl"></a><a name="ctreectrl"></a>CTreeCtrl::CTreeCtrl

Konstruuje `CTreeCtrl` obiekt.

```
CTreeCtrl();
```

## <a name="ctreectrldeleteallitems"></a><a name="deleteallitems"></a>CTreeCtrl::DeleteWszystkie elementy

Wywołanie tej funkcji, aby usunąć wszystkie elementy z kontrolki widoku drzewa.

```
BOOL DeleteAllItems();
```

### <a name="return-value"></a>Wartość zwracana

Nonzero jeśli się powiedzie; w przeciwnym razie 0.

### <a name="example"></a>Przykład

[!code-cpp[NVC_MFC_CTreeCtrl#3](../../mfc/reference/codesnippet/cpp/ctreectrl-class_3.cpp)]

## <a name="ctreectrldeleteitem"></a><a name="deleteitem"></a>CTreeCtrl::DeleteItem

Wywołanie tej funkcji, aby usunąć element z kontrolki widoku drzewa.

```
BOOL DeleteItem(HTREEITEM hItem);
```

### <a name="parameters"></a>Parametry

*hItem (własówce)*<br/>
Uchwyt elementu drzewa do usunięcia. Jeśli *hitem* ma wartość TVI_ROOT, wszystkie elementy są usuwane z kontrolki widoku drzewa.

### <a name="return-value"></a>Wartość zwracana

Nonzero jeśli się powiedzie; w przeciwnym razie 0.

### <a name="example"></a>Przykład

[!code-cpp[NVC_MFC_CTreeCtrl#4](../../mfc/reference/codesnippet/cpp/ctreectrl-class_4.cpp)]

## <a name="ctreectrleditlabel"></a><a name="editlabel"></a>CTreeCtrl::EditLabel

Wywołanie tej funkcji, aby rozpocząć edycję w miejscu tekstu określonego elementu.

```
CEdit* EditLabel(HTREEITEM hItem);
```

### <a name="parameters"></a>Parametry

*hItem (własówce)*<br/>
Uchwyt elementu drzewa do edycji.

### <a name="return-value"></a>Wartość zwracana

Jeśli się powiedzie, `CEdit` wskaźnik do obiektu, który jest używany do edycji tekstu elementu; w przeciwnym razie NULL.

### <a name="remarks"></a>Uwagi

Edycja odbywa się poprzez zastąpienie tekstu elementu formantem edycji jednowierszowej zawierającej tekst.

### <a name="example"></a>Przykład

[!code-cpp[NVC_MFC_CTreeCtrl#5](../../mfc/reference/codesnippet/cpp/ctreectrl-class_5.cpp)]

## <a name="ctreectrlendeditlabelnow"></a><a name="endeditlabelnow"></a>CTreeCtrl::EndEditLabelNow

Kończy operację edycji na etykiecie elementu widoku drzewa w bieżącym formancie widoku drzewa.

```
BOOL EndEditLabelNow(BOOL fCancelWithoutSave);
```

### <a name="parameters"></a>Parametry

|Parametr|Opis|
|---------------|-----------------|
|*fCancelWithoutSave*|[w] PRAWDA, aby odrzucić zmiany elementu widoku drzewa przed zakończeniem operacji edycji lub FALSE, aby zapisać zmiany w elemencie widoku drzewa przed zakończeniem operacji.|

### <a name="return-value"></a>Wartość zwracana

PRAWDA, jeśli ta metoda zakończy się pomyślnie; w przeciwnym razie FALSE.

### <a name="remarks"></a>Uwagi

Ta metoda wysyła komunikat [TVM_ENDEDITLABELNOW,](/windows/win32/Controls/tvm-endeditlabelnow) który jest opisany w windows SDK.

## <a name="ctreectrlensurevisible"></a><a name="ensurevisible"></a>CTreeCtrl::EnsureVisible

Wywołanie tej funkcji, aby upewnić się, że element widoku drzewa jest widoczny.

```
BOOL EnsureVisible(HTREEITEM hItem);
```

### <a name="parameters"></a>Parametry

*hItem (własówce)*<br/>
Uchwyt elementu drzewa jest widoczny.

### <a name="return-value"></a>Wartość zwracana

Zwraca wartość PRAWDA, jeśli system przewinął elementy w formancie widoku drzewa, aby upewnić się, że określony element jest widoczny. W przeciwnym razie zwracana wartość jest FAŁSZ.

### <a name="remarks"></a>Uwagi

W razie potrzeby funkcja rozwija element nadrzędny lub przewija formant widoku drzewa, tak aby element był widoczny.

### <a name="example"></a>Przykład

[!code-cpp[NVC_MFC_CTreeCtrl#6](../../mfc/reference/codesnippet/cpp/ctreectrl-class_6.cpp)]

## <a name="ctreectrlexpand"></a><a name="expand"></a>CTreeCtrl::Rozwiń

Wywołanie tej funkcji, aby rozwinąć lub zwinąć listę elementów podrzędnych, jeśli istnieje, skojarzone z danym elementem nadrzędnym.

```
BOOL Expand(
    HTREEITEM hItem,
    UINT nCode);
```

### <a name="parameters"></a>Parametry

*hItem (własówce)*<br/>
Uchwyt elementu drzewa jest rozwinięty.

*kod n*<br/>
Flaga wskazująca rodzaj działania, które należy podjąć. Ta flaga może mieć jedną z następujących wartości:

- TVE_COLLAPSE zwija listę.

- TVE_COLLAPSERESET Zwija listę i usuwa elementy podrzędne. Flaga stanu TVIS_EXPANDEDONCE zostanie zresetowana. Ta flaga musi być używana z flagą TVE_COLLAPSE.

- TVE_EXPAND Rozszerza listę.

- TVE_TOGGLE zwija listę, jeśli jest aktualnie rozwinięta lub rozwija ją, jeśli jest aktualnie zwinięta.

### <a name="return-value"></a>Wartość zwracana

Nonzero jeśli się powiedzie; w przeciwnym razie 0.

### <a name="example"></a>Przykład

  Zobacz przykład [CTreeCtrl::EnsureVisible](#ensurevisible).

## <a name="ctreectrlgetbkcolor"></a><a name="getbkcolor"></a>CTreeCtrl::GetBkColor

Ta funkcja elementu członkowskiego implementuje zachowanie [TVM_GETBKCOLOR](/windows/win32/Controls/tvm-getbkcolor)komunikatu Win32, zgodnie z opisem w windows SDK.

```
COLORREF GetBkColor() const;
```

### <a name="return-value"></a>Wartość zwracana

Wartość COLORREF reprezentująca bieżący kolor tła okna dla formantu. Jeśli ta wartość wynosi -1, formant używa koloru okna systemu. W takim przypadku można `::GetSysColor(COLOR_WINDOW)` użyć, aby uzyskać bieżący kolor systemu, który używa formantu.

### <a name="example"></a>Przykład

  Zobacz przykład [CTreeCtrl::SetTextColor](#settextcolor).

## <a name="ctreectrlgetcheck"></a><a name="getcheck"></a>CTreeCtrl::GetCheck

Wywołanie tej funkcji elementu członkowskiego, aby pobrać stan wyboru elementu.

```
BOOL GetCheck(HTREEITEM hItem) const;
```

### <a name="parameters"></a>Parametry

*hItem (własówce)*<br/>
HTREEITEM o których do otrzymania informacji o stanie.

### <a name="return-value"></a>Wartość zwracana

Niezerowe, jeśli element sterujący drzewa jest zaznaczony; w przeciwnym razie 0.

### <a name="example"></a>Przykład

  Zobacz przykład [CTreeCtrl::SetCheck](#setcheck).

## <a name="ctreectrlgetchilditem"></a><a name="getchilditem"></a>CTreeCtrl::GetChildItem

Wywołanie tej funkcji, aby pobrać element widoku drzewa, który jest elementem podrzędnym elementu określonego przez *hItem*.

```
HTREEITEM GetChildItem(HTREEITEM hItem) const;
```

### <a name="parameters"></a>Parametry

*hItem (własówce)*<br/>
Dojście elementu drzewa.

### <a name="return-value"></a>Wartość zwracana

Uchwyt elementu podrzędnego, jeśli zakończy się pomyślnie; w przeciwnym razie NULL.

### <a name="example"></a>Przykład

[!code-cpp[NVC_MFC_CTreeCtrl#7](../../mfc/reference/codesnippet/cpp/ctreectrl-class_7.cpp)]

## <a name="ctreectrlgetcount"></a><a name="getcount"></a>CTreeCtrl::GetCount

Wywołanie tej funkcji, aby pobrać liczbę elementów w formancie widoku drzewa.

```
UINT GetCount() const;
```

### <a name="return-value"></a>Wartość zwracana

Liczba elementów w formancie widoku drzewa.

### <a name="example"></a>Przykład

[!code-cpp[NVC_MFC_CTreeCtrl#8](../../mfc/reference/codesnippet/cpp/ctreectrl-class_8.cpp)]

## <a name="ctreectrlgetdrophilightitem"></a><a name="getdrophilightitem"></a>CTreeCtrl::GetDropHilightItem

Wywołanie tej funkcji, aby pobrać element, który jest celem operacji przeciągania i upuszczania.

```
HTREEITEM GetDropHilightItem() const;
```

### <a name="return-value"></a>Wartość zwracana

Uchwyt elementu porzucone, jeśli zakończy się pomyślnie; w przeciwnym razie NULL.

### <a name="example"></a>Przykład

[!code-cpp[NVC_MFC_CTreeCtrl#9](../../mfc/reference/codesnippet/cpp/ctreectrl-class_9.cpp)]

## <a name="ctreectrlgeteditcontrol"></a><a name="geteditcontrol"></a>CTreeCtrl::GetEditControl

Wywołanie tej funkcji, aby pobrać uchwyt formantu edycji używanego do edycji tekstu elementu widoku drzewa.

```
CEdit* GetEditControl() const;
```

### <a name="return-value"></a>Wartość zwracana

Wskaźnik do formantu edycji używany do edytowania tekstu elementu, jeśli zakończy się pomyślnie; w przeciwnym razie NULL.

### <a name="example"></a>Przykład

[!code-cpp[NVC_MFC_CTreeCtrl#10](../../mfc/reference/codesnippet/cpp/ctreectrl-class_10.cpp)]

## <a name="ctreectrlgetextendedstyle"></a><a name="getextendedstyle"></a>CTreeCtrl::GetExtendedStyle

Pobiera rozszerzone style, które jest używany bieżącego formantu widoku drzewa.

```
DWORD GetExtendedStyle() const;
```

### <a name="return-value"></a>Wartość zwracana

Wartość, która zawiera kombinację bitową (OR) stylów rozszerzonych bieżącego formantu widoku drzewa. Aby uzyskać więcej informacji, zobacz [Rozszerzone style kontrolki widoku drzewa](/windows/win32/Controls/tree-view-control-window-extended-styles).

### <a name="remarks"></a>Uwagi

Ta metoda wysyła komunikat [TVM_GETEXTENDEDSTYLE,](/windows/win32/Controls/tvm-getextendedstyle) który jest opisany w windows SDK.

## <a name="ctreectrlgetfirstvisibleitem"></a><a name="getfirstvisibleitem"></a>CTreeCtrl::GetFirstVisibleItem

Wywołanie tej funkcji, aby pobrać pierwszy widoczny element formantu widoku drzewa.

```
HTREEITEM GetFirstVisibleItem() const;
```

### <a name="return-value"></a>Wartość zwracana

Uchwyt pierwszego widocznego elementu; w przeciwnym razie NULL.

### <a name="example"></a>Przykład

  Zobacz przykład [CTreeCtrl::SetCheck](#setcheck).

## <a name="ctreectrlgetimagelist"></a><a name="getimagelist"></a>CTreeCtrl::Lista GetImage

Wywołanie tej funkcji, aby pobrać dojście normalnej lub stan listy obrazu skojarzone z formantem widoku drzewa.

```
CImageList* GetImageList(UINT nImageList) const;
```

### <a name="parameters"></a>Parametry

*nImageList (Lista)*<br/>
Typ listy obrazów do pobrania. Lista obrazów może być jedną z następujących wartości:

- TVSIL_NORMAL Pobiera normalną listę obrazów, która zawiera wybrane i nieselektyjowane obrazy dla elementu widoku drzewa.

- TVSIL_STATE Pobiera listę obrazów stanu, która zawiera obrazy dla elementów widoku drzewa, które są w stanie zdefiniowanym przez użytkownika.

### <a name="return-value"></a>Wartość zwracana

Wskaźnik do listy obrazów formantu, jeśli zakończy się pomyślnie; w przeciwnym razie NULL.

### <a name="remarks"></a>Uwagi

Każdy element w formancie widoku drzewa może mieć parę bitmapowych obrazów skojarzonych z nim. Jeden obraz jest wyświetlany po wybraniu elementu, a drugi jest wyświetlany, gdy element nie jest zaznaczony. Na przykład element może wyświetlać otwarty folder po zaznaczeniu i zamknięty folder, gdy nie jest zaznaczony.

Aby uzyskać więcej informacji na temat list obrazów, zobacz [CImageList](../../mfc/reference/cimagelist-class.md) klasy.

### <a name="example"></a>Przykład

[!code-cpp[NVC_MFC_CTreeCtrl#11](../../mfc/reference/codesnippet/cpp/ctreectrl-class_11.cpp)]

## <a name="ctreectrlgetindent"></a><a name="getindent"></a>CTreeCtrl::GetIndent

Wywołanie tej funkcji, aby pobrać kwotę, w pikselach, że elementy podrzędne są wcięte względem ich elementów nadrzędnych.

```
UINT GetIndent() const;
```

### <a name="return-value"></a>Wartość zwracana

Ilość wcięć mierzona w pikselach.

### <a name="example"></a>Przykład

[!code-cpp[NVC_MFC_CTreeCtrl#12](../../mfc/reference/codesnippet/cpp/ctreectrl-class_12.cpp)]

## <a name="ctreectrlgetinsertmarkcolor"></a><a name="getinsertmarkcolor"></a>CTreeCtrl::GetInsertMarkColor

Ta funkcja elementu członkowskiego implementuje zachowanie [TVM_GETINSERTMARKCOLOR](/windows/win32/Controls/tvm-getinsertmarkcolor)komunikatu Win32, zgodnie z opisem w windows SDK.

```
COLORREF GetInsertMarkColor() const;
```

### <a name="return-value"></a>Wartość zwracana

Wartość COLORREF zawierająca bieżący kolor znacznika wstawiania.

### <a name="example"></a>Przykład

[!code-cpp[NVC_MFC_CTreeCtrl#13](../../mfc/reference/codesnippet/cpp/ctreectrl-class_13.cpp)]

## <a name="ctreectrlgetitem"></a><a name="getitem"></a>CTreeCtrl::GetItem

Wywołanie tej funkcji, aby pobrać atrybuty określonego elementu widoku drzewa.

```
BOOL GetItem(TVITEM* pItem) const;
```

### <a name="parameters"></a>Parametry

*pItem (własówce)*<br/>
Wskaźnik do struktury [TVITEM,](/windows/win32/api/commctrl/ns-commctrl-tvitemw) zgodnie z opisem w windows SDK.

### <a name="return-value"></a>Wartość zwracana

Nonzero jeśli się powiedzie; w przeciwnym razie 0.

### <a name="example"></a>Przykład

  Zobacz przykład [CTreeCtrl::DeleteItem](#deleteitem).

## <a name="ctreectrlgetitemdata"></a><a name="getitemdata"></a>CTreeCtrl::GetItemData

Wywołanie tej funkcji, aby pobrać wartość specyficzne dla aplikacji skojarzone z określonym elementem.

```
DWORD_PTR GetItemData(HTREEITEM hItem) const;
```

### <a name="parameters"></a>Parametry

*hItem (własówce)*<br/>
Obsługa elementu, którego dane mają zostać pobrane.

### <a name="return-value"></a>Wartość zwracana

Wartość specyficzne dla aplikacji o rozmiarze wskaźnika skojarzona z elementem określonym przez *hItem*.

### <a name="example"></a>Przykład

[!code-cpp[NVC_MFC_CTreeCtrl#14](../../mfc/reference/codesnippet/cpp/ctreectrl-class_14.cpp)]

## <a name="ctreectrlgetitemexpandedimageindex"></a><a name="getitemexpandedimageindex"></a>CTreeCtrl::GetItemRozwińImdeks

Pobiera indeks obrazu do wyświetlenia, gdy określony element bieżącego formantu widoku drzewa jest w stanie rozwiniętym.

```
int GetItemExpandedImageIndex(HTREEITEM hItem)const;
```

### <a name="parameters"></a>Parametry

|Parametr|Opis|
|---------------|-----------------|
|*hItem (własówce)*|[w] Dojście do elementu sterującego widoku drzewa.|

### <a name="return-value"></a>Wartość zwracana

Indeks obrazu do wyświetlenia, gdy określony element jest w stanie rozwiniętym.

### <a name="remarks"></a>Uwagi

Ta metoda wysyła komunikat [TVM_GETITEM,](/windows/win32/Controls/tvm-getitem) który jest opisany w windows SDK. Ten komunikat zwraca [TVITEMEX](/windows/win32/api/commctrl/ns-commctrl-tvitemexw) struktury, która opisuje element kontroli widoku drzewa, a następnie ta metoda pobiera `iExpandedImage` element członkowski z tej struktury.

## <a name="ctreectrlgetitemheight"></a><a name="getitemheight"></a>CTreeCtrl::GetItemHeight

Ta funkcja elementu członkowskiego implementuje zachowanie [TVM_GETITEMHEIGHT](/windows/win32/Controls/tvm-getitemheight)komunikatu Win32, zgodnie z opisem w windows SDK.

```
SHORT GetItemHeight() const;
```

### <a name="return-value"></a>Wartość zwracana

Wysokość elementu w pikselach.

### <a name="example"></a>Przykład

[!code-cpp[NVC_MFC_CTreeCtrl#15](../../mfc/reference/codesnippet/cpp/ctreectrl-class_15.cpp)]

## <a name="ctreectrlgetitemimage"></a><a name="getitemimage"></a>CTreeCtrl::GetItemImage

Każdy element w formancie widoku drzewa może mieć parę bitmapowych obrazów skojarzonych z nim.

```
BOOL GetItemImage(
    HTREEITEM hItem,
    int& nImage,
    int& nSelectedImage) const;
```

### <a name="parameters"></a>Parametry

*hItem (własówce)*<br/>
Dojście elementu, którego obraz ma zostać pobrany.

*nImage (Obraz)*<br/>
Liczba całkowita, która odbiera indeks obrazu elementu w obrębie listy obrazów formantu widoku drzewa.

*nWybranyImage*<br/>
Liczba całkowita, która odbiera indeks wybranego obrazu elementu na liście obrazów formantu widoku drzewa.

### <a name="return-value"></a>Wartość zwracana

Nonzero jeśli się powiedzie; w przeciwnym razie 0.

### <a name="remarks"></a>Uwagi

Obrazy są wyświetlane po lewej stronie etykiety elementu. Jeden obraz jest wyświetlany po wybraniu elementu, a drugi jest wyświetlany, gdy element nie jest zaznaczony. Na przykład element może wyświetlać otwarty folder po zaznaczeniu i zamknięty folder, gdy nie jest zaznaczony.

Wywołanie tej funkcji, aby pobrać indeks obrazu elementu i jego wybranego obrazu w liście obrazów formantu widoku drzewa.

### <a name="example"></a>Przykład

[!code-cpp[NVC_MFC_CTreeCtrl#16](../../mfc/reference/codesnippet/cpp/ctreectrl-class_16.cpp)]

## <a name="ctreectrlgetitempartrect"></a><a name="getitempartrect"></a>CTreeCtrl::GetItemPartrect

Pobiera prostokąt ograniczający dla określonej części określonego elementu w bieżącym formancie widoku drzewa.

```
BOOL GetItemPartRect(
    HTREEITEM hItem,
    int nPart,
    LPRECT lpRect)const;
```

### <a name="parameters"></a>Parametry

|Parametr|Opis|
|---------------|-----------------|
|*hItem (własówce)*|[w] Dojście do elementu sterującego widoku drzewa.|
|*nPart (część)*|[w] Identyfikator części. Musi być ustawiona na TVGIPR_BUTTON.|
|*Lprect*|[na zewnątrz] Wskaźnik do struktury [RECT.](/previous-versions/dd162897\(v=vs.85\)) Jeśli ta metoda zakończy się pomyślnie, struktura odbiera współrzędne prostokąta części określonej przez *hItem* i *nPart*.|

### <a name="return-value"></a>Wartość zwracana

PRAWDA, jeśli ta metoda zakończy się pomyślnie; w przeciwnym razie FALSE.

### <a name="remarks"></a>Uwagi

Każdy element formantu drzewa jest ograniczony prostokątem grafiki. Za każdym razem, gdy punkt w tym prostokącie zostanie kliknięty, mówi się, że element jest *trafiony*. Ta metoda zwraca największy prostokąt w taki sposób, że po kliknięciu punktu w prostokącie element identyfikowany przez parametr *hItem* zostanie trafiony.

Ta metoda wysyła komunikat TVM_GETITEMPARTRECT, który jest opisany w windows SDK. Aby uzyskać więcej informacji, zobacz [TreeView_GetItemPartRect](/windows/win32/api/commctrl/nf-commctrl-treeview_getitempartrect) makra.

### <a name="example"></a>Przykład

Poniższy przykład kodu definiuje `m_treeCtrl`zmienną , która jest używana do uzyskiwania dostępu do bieżącego formantu widoku drzewa. Przykład kodu definiuje również niepodpisaną całkowitej i kilka zmiennych HTREEITEM. Te zmienne są używane w następnym przykładzie.

[!code-cpp[NVC_MFC_CTreeCtrl_s1#1](../../mfc/reference/codesnippet/cpp/ctreectrl-class_17.h)]

### <a name="example"></a>Przykład

Poniższy przykład kodu używa identyfikatora ułatwień dostępu i [CTreeCtrl::MapAccIdToItem](#mapaccidtoitem) metody pobierania dojścia do elementu widoku drzewa głównego. Następnie w przykładzie używa dojścia i [CTreeCtrl::GetItemPartRect](#getitempartrect) metody rysowania prostokąta 3D wokół tego elementu. We wcześniejszej sekcji przykładu kodu, który nie jest wyświetlany, utworzyliśmy widok drzewa, który składa się z węzła kraju/regionu głównego dla Stanów Zjednoczonych, węzłów podrzędnych dla stanów Pensylwania i Waszyngton oraz elementów drzewa dla miast w tych stanach. Użyliśmy [CTreeCtrl::MapItemToAccID](#mapitemtoaccid) metody skojarzyć element widoku drzewa głównego z identyfikatorem ułatwień dostępu.

[!code-cpp[NVC_MFC_CTreeCtrl_s1#5](../../mfc/reference/codesnippet/cpp/ctreectrl-class_18.cpp)]

## <a name="ctreectrlgetitemrect"></a><a name="getitemrect"></a>CTreeCtrl::GetItemRect

Wywołanie tej funkcji, aby pobrać prostokąt ograniczający dla *hItem* i określić, czy jest on widoczny, czy nie.

```
BOOL GetItemRect(
    HTREEITEM hItem,
    LPRECT lpRect,
    BOOL bTextOnly) const;
```

### <a name="parameters"></a>Parametry

*hItem (własówce)*<br/>
Dojście elementu sterującego widoku drzewa.

*Lprect*<br/>
Wskaźnik do struktury [RECT,](/previous-versions/dd162897\(v=vs.85\)) która odbiera prostokąt ograniczający. Współrzędne są względem lewego górnego rogu formantu widoku drzewa.

*bTeksa nie tylko*<br/>
Jeśli ten parametr jest niezerowy, prostokąt ograniczający zawiera tylko tekst elementu. W przeciwnym razie zawiera cały wiersz, który zajmuje element w formancie widoku drzewa.

### <a name="return-value"></a>Wartość zwracana

Niezerowe, jeśli element jest widoczny, z prostokątem ograniczającym zawartym w *lpRect*. W przeciwnym razie 0 z *lpRect* niezainicjowane.

### <a name="example"></a>Przykład

[!code-cpp[NVC_MFC_CTreeCtrl#17](../../mfc/reference/codesnippet/cpp/ctreectrl-class_19.cpp)]

## <a name="ctreectrlgetitemstate"></a><a name="getitemstate"></a>CTreeCtrl::GetItemState

Zwraca stan towaru określonego przez *hItem*.

```
UINT GetItemState(
    HTREEITEM hItem,
    UINT nStateMask) const;
```

### <a name="parameters"></a>Parametry

*hItem (własówce)*<br/>
Dojście towaru, którego stan ma zostać pobrany.

*nMask stanowa*<br/>
Maska wskazująca jeden lub więcej stanów do pobrania. Aby uzyskać więcej informacji na temat możliwych wartości dla `state` `stateMask` *nStateMask*, zobacz omówienie i członków struktury [TVITEM](/windows/win32/api/commctrl/ns-commctrl-tvitemw) w windows SDK.

### <a name="return-value"></a>Wartość zwracana

UINT, który przechowuje bitowe LUB wartości określonych przez nStateMask. Aby uzyskać informacje na temat możliwych wartości, zobacz [CTreeCtrl::GetItem](#getitem). Aby znaleźć wartość dla określonego stanu, wykonaj działanie bitowe AND wartości stanu i wartości zwracanej, jak pokazano w poniższym przykładzie.

### <a name="example"></a>Przykład

[!code-cpp[NVC_MFC_CTreeCtrl#18](../../mfc/reference/codesnippet/cpp/ctreectrl-class_20.cpp)]

## <a name="ctreectrlgetitemstateex"></a><a name="getitemstateex"></a>CTreeCtrl::GetItemStateEx

Pobiera rozszerzony stan określonego elementu w bieżącym formancie widoku drzewa.

```
UINT GetItemStateEx(HTREEITEM hItem) const;
```

### <a name="parameters"></a>Parametry

|Parametr|Opis|
|---------------|-----------------|
|*hItem (własówce)*|[w] Dojście do elementu sterującego widoku drzewa.|

### <a name="return-value"></a>Wartość zwracana

Rozszerzony stan elementu. Aby uzyskać więcej `uStateEx` informacji, zobacz element struktury [TVITEMEX.](/windows/win32/api/commctrl/ns-commctrl-tvitemexw)

### <a name="remarks"></a>Uwagi

Ta metoda wysyła komunikat [TVM_GETITEM,](/windows/win32/Controls/tvm-getitem) który jest opisany w windows SDK. Ten komunikat zwraca [TVITEMEX](/windows/win32/api/commctrl/ns-commctrl-tvitemexw) struktury, która opisuje element kontroli widoku `uStateEx` drzewa, a ta metoda pobiera element członkowski z tej struktury.

## <a name="ctreectrlgetitemtext"></a><a name="getitemtext"></a>CTreeCtrl::GetItemText

Zwraca tekst elementu określonego przez *hItem*.

```
CString GetItemText(HTREEITEM hItem) const;
```

### <a name="parameters"></a>Parametry

*hItem (własówce)*<br/>
Dojście elementu, którego tekst ma zostać pobrany.

### <a name="return-value"></a>Wartość zwracana

Obiekt `CString` zawierający tekst elementu.

### <a name="example"></a>Przykład

  Zobacz przykład [CTreeCtrl::GetNextItem](#getnextitem).

## <a name="ctreectrlgetlastvisibleitem"></a><a name="getlastvisibleitem"></a>CTreeCtrl::GetLastVisibleItem

Pobiera ostatni element węzła nierozwinięty w bieżącym formancie widoku drzewa.

```
HTREEITEM GetLastVisibleItem() const;
```

### <a name="return-value"></a>Wartość zwracana

Dojście do ostatniego elementu węzła nierozwinięty, jeśli metoda zakończy się pomyślnie; w przeciwnym razie NULL.

### <a name="remarks"></a>Uwagi

Ta metoda wysyła komunikat [TVM_GETNEXTITEM,](/windows/win32/Controls/tvm-getnextitem) który jest opisany w windows SDK. Aby uzyskać więcej informacji, zobacz flagę TVGN_LASTVISIBLE w parametrze *flagi* tej wiadomości.

### <a name="example"></a>Przykład

Poniższy przykład kodu definiuje `m_treeCtrl`zmienną , która jest używana do uzyskiwania dostępu do bieżącego formantu widoku drzewa. Przykład kodu definiuje również niepodpisaną całkowitej i kilka zmiennych HTREEITEM. Co najmniej jedna z tych zmiennych są używane w następnym przykładzie.

[!code-cpp[NVC_MFC_CTreeCtrl_s1#1](../../mfc/reference/codesnippet/cpp/ctreectrl-class_17.h)]

### <a name="example"></a>Przykład

Poniższy przykład kodu pobiera dojście do ostatniego elementu węzła widoku drzewa, a następnie rysuje prostokąt 3D wokół tego elementu. We wcześniejszej sekcji przykładu kodu, który nie jest wyświetlany, utworzyliśmy widok drzewa, który składa się z węzła kraju/regionu głównego dla Stanów Zjednoczonych, węzłów podrzędnych dla stanów Pensylwania i Waszyngton oraz elementów drzewa dla miast w tych stanach.

[!code-cpp[NVC_MFC_CTreeCtrl_s1#6](../../mfc/reference/codesnippet/cpp/ctreectrl-class_21.cpp)]

## <a name="ctreectrlgetlinecolor"></a><a name="getlinecolor"></a>CTreeCtrl::GetLineColor

Ta funkcja elementu członkowskiego implementuje zachowanie [TVM_GETLINECOLOR](/windows/win32/Controls/tvm-getlinecolor)komunikatu Win32, zgodnie z opisem w windows SDK.

```
COLORREF GetLineColor() const;
```

### <a name="return-value"></a>Wartość zwracana

Bieżący kolor linii.

### <a name="example"></a>Przykład

[!code-cpp[NVC_MFC_CTreeCtrl#19](../../mfc/reference/codesnippet/cpp/ctreectrl-class_22.cpp)]

## <a name="ctreectrlgetnextitem"></a><a name="getnextitem"></a>CTreeCtrl::GetNextItem

Wywołanie tej funkcji, aby pobrać element widoku drzewa, który ma określoną relację, wskazaną przez parametr *nCode,* do *hItem*.

```
HTREEITEM GetNextItem(
    HTREEITEM hItem,
    UINT nCode) const;
```

### <a name="parameters"></a>Parametry

*hItem (własówce)*<br/>
Dojście elementu drzewa.

*kod n*<br/>
Flaga wskazująca typ relacji z *hItem*. Ta flaga może być jedną z następujących wartości:

- TVGN_CARET Pobiera aktualnie zaznaczony element.

- TVGN_CHILD Pobiera pierwszy element podrzędny elementu określonego przez parametr *hItem.*

- TVGN_DROPHILITE Pobiera element, który jest celem operacji przeciągania i upuszczania.

- TVGN_FIRSTVISIBLE Pobiera pierwszy widoczny element.

- TVGN_LASTVISIBLE Pobiera ostatni rozwinięty element w drzewie. Nie spowoduje to pobrania ostatniego elementu widocznego w oknie widoku drzewa.

- TVGN_NEXT Pobiera następny element równorzędny.

- TVGN_NEXTVISIBLE Pobiera następny widoczny element, który następuje po określonym elemencie.

- TVGN_PARENT Pobiera element nadrzędny określonego elementu.

- TVGN_PREVIOUS Pobiera poprzedni element równorzędny.

- TVGN_PREVIOUSVISIBLE Pobiera pierwszy widoczny element, który poprzedza określony element.

- TVGN_ROOT Pobiera pierwszy element podrzędny elementu głównego, którego częścią jest określony element.

### <a name="return-value"></a>Wartość zwracana

Uchwyt następnego elementu, jeśli zakończy się pomyślnie; w przeciwnym razie NULL.

### <a name="remarks"></a>Uwagi

Ta funkcja zwróci wartość NULL, jeśli pobierany element jest węzłem głównym drzewa. Na przykład jeśli używasz tej wiadomości z flagą TVGN_PARENT na podrzędnym pierwszym poziomie węzła głównego widoku drzewa, wiadomość zwróci wartość NULL.

### <a name="example"></a>Przykład

Na przykład użycia `GetNextItem` w pętli zobacz [CTreeCtrl::DeleteItem](#deleteitem).

[!code-cpp[NVC_MFC_CTreeCtrl#20](../../mfc/reference/codesnippet/cpp/ctreectrl-class_23.cpp)]

## <a name="ctreectrlgetnextsiblingitem"></a><a name="getnextsiblingitem"></a>CTreeCtrl::GetNextSiblingItem

Wywołanie tej funkcji, aby pobrać następne rodzeństwo *hItem*.

```
HTREEITEM GetNextSiblingItem(HTREEITEM hItem) const;
```

### <a name="parameters"></a>Parametry

*hItem (własówce)*<br/>
Dojście elementu drzewa.

### <a name="return-value"></a>Wartość zwracana

Dojście następnego elementu równorzędnego; w przeciwnym razie NULL.

### <a name="example"></a>Przykład

[!code-cpp[NVC_MFC_CTreeCtrl#21](../../mfc/reference/codesnippet/cpp/ctreectrl-class_24.cpp)]

## <a name="ctreectrlgetnextvisibleitem"></a><a name="getnextvisibleitem"></a>CTreeCtrl::GetNextVisibleItem

Wywołanie tej funkcji, aby pobrać następny widoczny element *hItem*.

```
HTREEITEM GetNextVisibleItem(HTREEITEM hItem) const;
```

### <a name="parameters"></a>Parametry

*hItem (własówce)*<br/>
Dojście elementu drzewa.

### <a name="return-value"></a>Wartość zwracana

Uchwyt następnego widocznego elementu; w przeciwnym razie NULL.

### <a name="example"></a>Przykład

  Zobacz przykład [CTreeCtrl::SetCheck](#setcheck).

## <a name="ctreectrlgetparentitem"></a><a name="getparentitem"></a>CTreeCtrl::GetParentItem

Wywołanie tej funkcji, aby pobrać element nadrzędny *hItem*.

```
HTREEITEM GetParentItem(HTREEITEM hItem) const;
```

### <a name="parameters"></a>Parametry

*hItem (własówce)*<br/>
Dojście elementu drzewa.

### <a name="return-value"></a>Wartość zwracana

Dojście elementu nadrzędnego; w przeciwnym razie NULL.

### <a name="remarks"></a>Uwagi

Ta funkcja zwróci wartość NULL, jeśli element nadrzędny określonego elementu jest węzłem głównym drzewa.

### <a name="example"></a>Przykład

  Zobacz przykład [CTreeCtrl::EnsureVisible](#ensurevisible).

## <a name="ctreectrlgetprevsiblingitem"></a><a name="getprevsiblingitem"></a>CTreeCtrl::GetPrevSiblingItem

Wywołanie tej funkcji, aby pobrać poprzednie rodzeństwo *hItem*.

```
HTREEITEM GetPrevSiblingItem(HTREEITEM hItem) const;
```

### <a name="parameters"></a>Parametry

*hItem (własówce)*<br/>
Dojście elementu drzewa.

### <a name="return-value"></a>Wartość zwracana

Dojście poprzedniego elementu równorzędnego; w przeciwnym razie NULL.

### <a name="example"></a>Przykład

[!code-cpp[NVC_MFC_CTreeCtrl#22](../../mfc/reference/codesnippet/cpp/ctreectrl-class_25.cpp)]

## <a name="ctreectrlgetprevvisibleitem"></a><a name="getprevvisibleitem"></a>CTreeCtrl::GetPrevVisibleItem

Wywołanie tej funkcji, aby pobrać poprzedni widoczny element *hItem*.

```
HTREEITEM GetPrevVisibleItem(HTREEITEM hItem) const;
```

### <a name="parameters"></a>Parametry

*hItem (własówce)*<br/>
Dojście elementu drzewa.

### <a name="return-value"></a>Wartość zwracana

Uchwyt poprzedniego widocznego elementu; w przeciwnym razie NULL.

### <a name="example"></a>Przykład

[!code-cpp[NVC_MFC_CTreeCtrl#23](../../mfc/reference/codesnippet/cpp/ctreectrl-class_26.cpp)]

## <a name="ctreectrlgetrootitem"></a><a name="getrootitem"></a>CTreeCtrl::GetRootItem

Wywołanie tej funkcji, aby pobrać element główny formantu widoku drzewa.

```
HTREEITEM GetRootItem() const;
```

### <a name="return-value"></a>Wartość zwracana

Dojście elementu głównego; w przeciwnym razie NULL.

### <a name="example"></a>Przykład

  Zobacz przykład [CTreeCtrl::EditLabel](#editlabel).

## <a name="ctreectrlgetscrolltime"></a><a name="getscrolltime"></a>CTreeCtrl::GetScrollTime

Wywołanie tej funkcji elementu członkowskiego, aby pobrać maksymalny czas przewijania dla formantu widoku drzewa.

```
UINT GetScrollTime() const;
```

### <a name="return-value"></a>Wartość zwracana

Maksymalny czas przewijania w milisekundach.

### <a name="remarks"></a>Uwagi

Ta funkcja elementu członkowskiego implementuje zachowanie [TVM_GETSCROLLTIME](/windows/win32/Controls/tvm-getscrolltime)komunikatu Win32, zgodnie z opisem w windows SDK.

## <a name="ctreectrlgetselectedcount"></a><a name="getselectedcount"></a>CTreeCtrl::GetSelectedCount

Pobiera liczbę wybranych elementów w bieżącym formancie widoku drzewa.

```
UINT GetSelectedCount();
```

### <a name="return-value"></a>Wartość zwracana

Liczba wybranych elementów.

### <a name="remarks"></a>Uwagi

Ta metoda wysyła komunikat [TVM_GETSELECTEDCOUNT,](/windows/win32/Controls/tvm-getselectedcount) który jest opisany w windows SDK.

## <a name="ctreectrlgetselecteditem"></a><a name="getselecteditem"></a>CTreeCtrl::GetSelectedItem

Wywołanie tej funkcji, aby pobrać aktualnie wybrany element formantu widoku drzewa.

```
HTREEITEM GetSelectedItem() const;
```

### <a name="return-value"></a>Wartość zwracana

Uchwyt wybranego elementu; w przeciwnym razie NULL.

### <a name="example"></a>Przykład

[!code-cpp[NVC_MFC_CTreeCtrl#24](../../mfc/reference/codesnippet/cpp/ctreectrl-class_27.cpp)]

## <a name="ctreectrlgettextcolor"></a><a name="gettextcolor"></a>CTreeCtrl::GetTextColor

Ta funkcja elementu członkowskiego implementuje zachowanie [TVM_GETTEXTCOLOR](/windows/win32/Controls/tvm-gettextcolor)komunikatu Win32, zgodnie z opisem w windows SDK.

```
COLORREF GetTextColor() const;
```

### <a name="return-value"></a>Wartość zwracana

Wartość COLORREF reprezentująca bieżący kolor tekstu. Jeśli ta wartość wynosi -1, formant używa koloru systemowego dla koloru tekstu.

### <a name="example"></a>Przykład

  Zobacz przykład [CTreeCtrl::SetTextColor](#settextcolor).

## <a name="ctreectrlgettooltips"></a><a name="gettooltips"></a>CTreeCtrl::Poradek GetToolTips

Ta funkcja elementu członkowskiego implementuje zachowanie [TVM_GETTOOLTIPS](/windows/win32/Controls/tvm-gettooltips)komunikatu Win32, zgodnie z opisem w windows SDK.

```
CToolTipCtrl* GetToolTips() const;
```

### <a name="return-value"></a>Wartość zwracana

Wskaźnik do [CToolTipCtrl](../../mfc/reference/ctooltipctrl-class.md) obiektu, który ma być używany przez formant drzewa. Jeśli funkcja [Create](#create) element członkowski używa stylu TVS_NOTOOLTIPS, nie są używane żadne etykietki narzędzi i zwracana jest wartość NULL.

### <a name="remarks"></a>Uwagi

Implementacja MFC `GetToolTips` zwraca `CToolTipCtrl` obiekt, który jest używany przez formant drzewa, a nie dojście do formantu etykietki narzędzia.

### <a name="example"></a>Przykład

[!code-cpp[NVC_MFC_CTreeCtrl#25](../../mfc/reference/codesnippet/cpp/ctreectrl-class_28.cpp)]

## <a name="ctreectrlgetvisiblecount"></a><a name="getvisiblecount"></a>CTreeCtrl::GetVisibleCount

Wywołanie tej funkcji, aby pobrać liczbę widocznych elementów w formancie widoku drzewa.

```
UINT GetVisibleCount() const;
```

### <a name="return-value"></a>Wartość zwracana

Liczba widocznych elementów w formancie widoku drzewa; w przeciwnym razie - 1.

### <a name="example"></a>Przykład

  Zobacz przykład [CTreeCtrl::SetCheck](#setcheck).

## <a name="ctreectrlhittest"></a><a name="hittest"></a>CTreeCtrl::HitTest

Wywołanie tej funkcji, aby określić lokalizację określonego punktu względem obszaru klienta formantu widoku drzewa.

```
HTREEITEM HitTest(
    CPoint pt,
    UINT* pFlags = NULL) const;

HTREEITEM HitTest(TVHITTESTINFO* pHitTestInfo) const;
```

### <a name="parameters"></a>Parametry

*Pt*<br/>
Współrzędne klienta punktu do przetestowania.

*pFlags*<br/>
Wskaźnik do liczby całkowitej, która odbiera informacje o wynikach testu trafień. Może to być jedna lub więcej `flags` wartości wymienionych w obszarze członkowskim w sekcji Uwagi.

*pHitTestInfo*<br/>
Adres struktury [TVHITTESTINFO,](/windows/win32/api/commctrl/ns-commctrl-tvhittestinfo) która zawiera pozycję do trafienia testu i która otrzymuje informacje o wynikach testu trafień.

### <a name="return-value"></a>Wartość zwracana

Uchwyt elementu widoku drzewa, który zajmuje określony punkt lub NULL, jeśli żaden element nie zajmuje punktu.

### <a name="remarks"></a>Uwagi

Gdy ta funkcja jest wywoływana, *pt* parametr określa współrzędne punktu do testowania. Funkcja zwraca uchwyt towaru w określonym punkcie lub NULL, jeśli żaden element nie zajmuje punktu. Ponadto *pFlags* parametr zawiera wartość, która wskazuje lokalizację określonego punktu. Możliwe wartości:

|||
|-|-|
|Wartość|Znaczenie|
|TVHT_ABOVE|Nad obszarem klienta.|
|TVHT_BELOW|Poniżej obszaru klienta.|
|TVHT_NOWHERE|W obszarze klienta, ale poniżej ostatniego elementu.|
|TVHT_ONITEM|Na mapie bitowej lub etykiecie skojarzonej z elementem.|
|TVHT_ONITEMBUTTON|Na przycisku skojarzonym z elementem.|
|TVHT_ONITEMICON|Na mapie bitowej skojarzonej z elementem.|
|TVHT_ONITEMINDENT|W wcięciach skojarzonych z elementem.|
|TVHT_ONITEMLABEL|Na etykiecie (ciąg) skojarzony z elementem.|
|TVHT_ONITEMRIGHT|W obszarze po prawej stronie elementu.|
|TVHT_ONITEMSTATEICON|Na ikonie stanu dla elementu widoku drzewa, który jest w stanie zdefiniowanym przez użytkownika.|
|TVHT_TOLEFT|Po lewej stronie obszaru klienta.|
|TVHT_TORIGHT|Po prawej stronie obszaru klienta.|
|||

### <a name="example"></a>Przykład

[!code-cpp[NVC_MFC_CTreeCtrl#26](../../mfc/reference/codesnippet/cpp/ctreectrl-class_29.cpp)]

## <a name="ctreectrlinsertitem"></a><a name="insertitem"></a>CTreeCtrl::InsertItem

Wywołanie tej funkcji, aby wstawić nowy element w formancie widoku drzewa.

```
HTREEITEM InsertItem(LPTVINSERTSTRUCT lpInsertStruct);

HTREEITEM InsertItem(
    UINT nMask,
    LPCTSTR lpszItem,
    int nImage,
    int nSelectedImage,
    UINT nState,
    UINT nStateMask,
    LPARAM lParam,
    HTREEITEM hParent,
    HTREEITEM hInsertAfter);

HTREEITEM InsertItem(
    LPCTSTR lpszItem,
    HTREEITEM hParent = TVI_ROOT,
    HTREEITEM hInsertAfter = TVI_LAST);

HTREEITEM InsertItem(
    LPCTSTR lpszItem,
    int nImage,
    int nSelectedImage,
    HTREEITEM hParent = TVI_ROOT,
    HTREEITEM hInsertAfter = TVI_LAST);
```

### <a name="parameters"></a>Parametry

*lpInsertStruct (Struktura lpInsertStruct)*<br/>
Wskaźnik `TVINSERTSTRUCT` do, który określa atrybuty elementu widoku drzewa, który ma zostać wstawiony.

*nMask*<br/>
Integer określający atrybuty, które mają być ustawione. Zobacz `TVITEM` strukturę w windows SDK.

*lpszItem*<br/>
Adres ciągu zawierającego tekst elementu.

*nImage (Obraz)*<br/>
Indeks obrazu elementu na liście obrazów formantu widoku drzewa.

*nWybranyImage*<br/>
Indeks wybranego obrazu elementu na liście obrazów formantu widoku drzewa.

*nPaństwo*<br/>
Określa wartości stanów elementu. Zobacz Stan elementu kontroli widoku drzewa w zestawie Windows SDK, aby uzyskać listę odpowiednich stanów.

*nMask stanowa*<br/>
Określa, które stany mają być ustawione. Zobacz `TVITEM` strukturę w windows SDK.

*Lparam*<br/>
Wartość specyficzne dla aplikacji o rozmiarze wskaźnika skojarzona z elementem.

*hRoszt*<br/>
Uchwyt elementu nadrzędnego wstawionego elementu.

*hInsertPo*<br/>
Uchwyt towaru, po którym ma zostać wstawiony nowy element.

### <a name="return-value"></a>Wartość zwracana

Uchwyt nowego elementu, jeśli zakończy się pomyślnie; w przeciwnym razie NULL.

### <a name="remarks"></a>Uwagi

W przykładzie przedstawiono sytuacje, w których można użyć każdej wersji funkcji podczas wstawiania elementu formantu drzewa.

### <a name="example"></a>Przykład

[!code-cpp[NVC_MFC_CTreeCtrl#27](../../mfc/reference/codesnippet/cpp/ctreectrl-class_30.cpp)]

## <a name="ctreectrlitemhaschildren"></a><a name="itemhaschildren"></a>CTreeCtrl::ItemHasChildren

Ta funkcja służy do określenia, czy element drzewa określony przez *hItem* ma elementy podrzędne.

```
BOOL ItemHasChildren(HTREEITEM hItem) const;
```

### <a name="parameters"></a>Parametry

*hItem (własówce)*<br/>
Dojście elementu drzewa.

### <a name="return-value"></a>Wartość zwracana

Nonzero, jeśli element drzewa określony przez *hItem* ma elementy podrzędne; 0, jeśli nie.

### <a name="remarks"></a>Uwagi

Jeśli tak, można następnie użyć [CTreeCtrl::GetChildItem](#getchilditem) do pobrania tych elementów podrzędnych.

### <a name="example"></a>Przykład

  Zobacz przykład [CTreeCtrl::GetSelectedItem](#getselecteditem).

## <a name="ctreectrlmapaccidtoitem"></a><a name="mapaccidtoitem"></a>CTreeCtrl::MapAccIdToItem

Mapuje określony identyfikator ułatwień dostępu do dojścia elementu widoku drzewa w bieżącym formancie widoku drzewa.

```
HTREEITEM MapAccIdToItem(UINT uAccId) const;
```

### <a name="parameters"></a>Parametry

|Parametr|Opis|
|---------------|-----------------|
|*uAccId*|[w] Identyfikator ułatwień dostępu dla elementu w elemencie widoku drzewa.|

### <a name="return-value"></a>Wartość zwracana

Dojście do elementu widoku `HTREEITEM`drzewa ( ), który odpowiada parametrowi *uAccId.* Aby uzyskać więcej informacji, zobacz *hItem* członek struktury [TVITEMEX.](/windows/win32/api/commctrl/ns-commctrl-tvitemexw)

### <a name="remarks"></a>Uwagi

Pomoce ułatwień dostępu to aplikacje, które pomagają osobom niepełnosprawnym korzystać z komputerów. Identyfikator ułatwień dostępu jest `IAccessible` używany przez interfejs do jednoznacznego określenia elementu w oknie. Aby uzyskać więcej informacji na temat identyfikatorów ułatwień dostępu, wyszukaj temat "Pomoc techniczna dotycząca aktywnych ułatwień dostępu" w [witrynie Microsoft Developer Network](https://go.microsoft.com/fwlink/p/?linkid=56322).

Ta metoda wysyła komunikat [TVM_MAPACCIDTOHTREEITEM,](/windows/win32/Controls/tvm-mapaccidtohtreeitem) który jest opisany w windows SDK.

### <a name="example"></a>Przykład

Poniższy przykład kodu definiuje `m_treeCtrl`zmienną , która jest używana do uzyskiwania dostępu do bieżącego formantu widoku drzewa. Przykład kodu definiuje również niepodpisaną całkowitej i kilka zmiennych HTREEITEM. Te zmienne są używane w następnym przykładzie.

[!code-cpp[NVC_MFC_CTreeCtrl_s1#1](../../mfc/reference/codesnippet/cpp/ctreectrl-class_17.h)]

### <a name="example"></a>Przykład

Poniższy przykład kodu używa identyfikatora ułatwień dostępu i [CTreeCtrl::MapAccIdToItem](#mapaccidtoitem) metody pobierania dojścia do elementu widoku drzewa głównego. W przykładzie używa dojścia i [CTreeCtrl::GetItemPartRect](#getitempartrect) metody rysowania prostokąta 3D wokół tego elementu. We wcześniejszej sekcji przykładu kodu, który nie jest wyświetlany, utworzyliśmy widok drzewa, który składa się z węzła kraju/regionu głównego dla Stanów Zjednoczonych, węzłów podrzędnych dla stanów Pensylwania i Waszyngton oraz elementów drzewa dla miast w tych stanach. Użyliśmy [CTreeCtrl::MapItemToAccID](#mapitemtoaccid) metody skojarzyć element widoku drzewa głównego z identyfikatorem ułatwień dostępu.

[!code-cpp[NVC_MFC_CTreeCtrl_s1#5](../../mfc/reference/codesnippet/cpp/ctreectrl-class_18.cpp)]

## <a name="ctreectrlmapitemtoaccid"></a><a name="mapitemtoaccid"></a>CTreeCtrl::MapItemToAccID

Mapuje określony uchwyt elementu widoku drzewa w bieżącym formancie widoku drzewa do identyfikatora ułatwień dostępu.

```
UINT MapItemToAccID(HTREEITEM hItem) const;
```

### <a name="parameters"></a>Parametry

|Parametr|Opis|
|---------------|-----------------|
|*hItem (własówce)*|[w] Dojście elementu widoku drzewa w formancie. Aby uzyskać więcej informacji, zobacz *hItem* członek struktury [TVITEMEX.](/windows/win32/api/commctrl/ns-commctrl-tvitemexw)|

### <a name="return-value"></a>Wartość zwracana

Identyfikator ułatwień dostępu, który odpowiada *parametrowi hItem.*

### <a name="remarks"></a>Uwagi

Pomoce ułatwień dostępu to aplikacje, które pomagają osobom niepełnosprawnym korzystać z komputerów. Identyfikator ułatwień dostępu jest `IAccessible` używany przez interfejs do jednoznacznego określenia elementu w oknie. Aby uzyskać więcej informacji na temat identyfikatorów ułatwień dostępu, wyszukaj temat "Pomoc techniczna dotycząca aktywnych ułatwień dostępu" w [witrynie Microsoft Developer Network](https://go.microsoft.com/fwlink/p/?linkid=56322).

Ta metoda wysyła komunikat [TVM_MAPHTREEITEMTOACCID,](/windows/win32/Controls/tvm-maphtreeitemtoaccid) który jest opisany w windows SDK.

### <a name="example"></a>Przykład

Poniższy przykład kodu definiuje `m_treeCtrl`zmienną , która jest używana do uzyskiwania dostępu do bieżącego formantu widoku drzewa. Przykład kodu definiuje również niepodpisaną całkowitej i kilka zmiennych HTREEITEM. Te zmienne są używane w następnym przykładzie.

[!code-cpp[NVC_MFC_CTreeCtrl_s1#1](../../mfc/reference/codesnippet/cpp/ctreectrl-class_17.h)]

### <a name="example"></a>Przykład

Poniższy przykład kodu uzyskuje numer identyfikacyjny dla elementu kontroli widoku drzewa. We wcześniejszej sekcji przykładu kodu, który nie jest wyświetlany, utworzyliśmy widok drzewa, który składa się z węzła kraju/regionu głównego dla Stanów Zjednoczonych, węzłów podrzędnych dla stanów Pensylwania i Waszyngton oraz elementów drzewa dla miast w tych stanach. W tym przykładzie kodu uzyskuje unikatowy numer identyfikacyjny dla węzła kraju/regionu głównego.

[!code-cpp[NVC_MFC_CTreeCtrl_s1#2](../../mfc/reference/codesnippet/cpp/ctreectrl-class_31.cpp)]

## <a name="ctreectrlselect"></a><a name="select"></a>CTreeCtrl::Wybierz

Wywołanie tej funkcji, aby wybrać dany element widoku drzewa, przewinąć element do widoku lub przerysować element w stylu używanym do wskazania obiektu docelowego operacji przeciągania i upuszczania.

```
BOOL Select(
    HTREEITEM hItem,
    UINT nCode);
```

### <a name="parameters"></a>Parametry

*hItem (własówce)*<br/>
Dojście elementu drzewa.

*kod n*<br/>
Rodzaj działań do podjęcia. Ten parametr może być jedną z następujących wartości:

- TVGN_CARET Ustawia zaznaczenie na dany element.

- TVGN_DROPHILITE Ponownie rysuje dany element w stylu używanym do wskazania celu operacji przeciągania i upuszczania.

- TVGN_FIRSTVISIBLE przewija widok drzewa w pionie, tak aby dany element był pierwszym widocznym elementem.

### <a name="return-value"></a>Wartość zwracana

Nonzero jeśli się powiedzie; w przeciwnym razie 0.

### <a name="remarks"></a>Uwagi

Jeśli *kod nCode* zawiera wartość TVGN_CARET, okno nadrzędne odbiera komunikaty powiadomień TVN_SELCHANGING i TVN_SELCHANGED. Ponadto jeśli określony element jest elementem podrzędnym zwiniętego elementu nadrzędnego, lista podrzędnych elementu podrzędnego rodzica jest rozwijana w celu ujawnienia określonego elementu. W takim przypadku okno nadrzędne odbiera komunikaty powiadomień TVN_ITEMEXPANDING i TVN_ITEMEXPANDED.

### <a name="example"></a>Przykład

  Zobacz przykład [CTreeCtrl::HitTest](#hittest).

## <a name="ctreectrlselectdroptarget"></a><a name="selectdroptarget"></a>CTreeCtrl::SelectDropTarget

Wywołanie tej funkcji, aby ponownie rysować element w stylu używanym do wskazania obiektu docelowego operacji przeciągania i upuszczania.

```
BOOL SelectDropTarget(HTREEITEM hItem);
```

### <a name="parameters"></a>Parametry

*hItem (własówce)*<br/>
Dojście elementu drzewa.

### <a name="return-value"></a>Wartość zwracana

Nonzero jeśli się powiedzie; w przeciwnym razie 0.

### <a name="example"></a>Przykład

[!code-cpp[NVC_MFC_CTreeCtrl#9](../../mfc/reference/codesnippet/cpp/ctreectrl-class_9.cpp)]

## <a name="ctreectrlselectitem"></a><a name="selectitem"></a>CTreeCtrl::SelectItem

Wywołanie tej funkcji, aby wybrać dany element widoku drzewa.

```
BOOL SelectItem(HTREEITEM hItem);
```

### <a name="parameters"></a>Parametry

*hItem (własówce)*<br/>
Dojście elementu drzewa.

### <a name="return-value"></a>Wartość zwracana

Nonzero jeśli się powiedzie; w przeciwnym razie 0.

### <a name="remarks"></a>Uwagi

Jeśli *hItem* ma wartość NULL, ta funkcja nie wybiera elementu.

### <a name="example"></a>Przykład

[!code-cpp[NVC_MFC_CTreeCtrl#26](../../mfc/reference/codesnippet/cpp/ctreectrl-class_29.cpp)]

## <a name="ctreectrlselectsetfirstvisible"></a><a name="selectsetfirstvisible"></a>CTreeCtrl::SelectSetFirstVisible

Wywołanie tej funkcji, aby przewinąć widok drzewa w pionie, tak aby dany element był pierwszym widocznym elementem.

```
BOOL SelectSetFirstVisible(HTREEITEM hItem);
```

### <a name="parameters"></a>Parametry

*hItem (własówce)*<br/>
Uchwyt elementu drzewa, który ma być ustawiony jako pierwszy widoczny element.

### <a name="return-value"></a>Wartość zwracana

Nonzero jeśli się powiedzie; w przeciwnym razie 0.

### <a name="remarks"></a>Uwagi

Funkcja wysyła wiadomość do okna z parametrami TVM_SELECTITEM i TVGN_FIRSTVISIBLE wiadomości.

### <a name="example"></a>Przykład

[!code-cpp[NVC_MFC_CTreeCtrl#28](../../mfc/reference/codesnippet/cpp/ctreectrl-class_32.cpp)]

## <a name="ctreectrlsetautoscrollinfo"></a><a name="setautoscrollinfo"></a>CTreeCtrl::SetAutoscrollInfo

Ustawia szybkość automatycznego podglądu bieżącej kontrolki widoku drzewa.

```
BOOL SetAutoscrollInfo(
    UINT uPixelsPerSec,
    UINT uUpdateTime);
```

### <a name="parameters"></a>Parametry

|Parametr|Opis|
|---------------|-----------------|
|*uPixelsPerSec*|[w] Liczba pikseli na sekundę do przewijania.|
|*uUpdateTime*|[w] Przedział czasu między aktualizacjami formantu.|

### <a name="return-value"></a>Wartość zwracana

Zawsze zwraca wartość PRAWDA.

### <a name="remarks"></a>Uwagi

Parametry autoprzeciwki służą do przewijania do widoku elementu, który obecnie nie jest widoczny. Formant widoku drzewa musi mieć TVS_EX_AUTOHSCROLL styl rozszerzony, który jest opisany w [stylach rozszerzonych widoku drzewa.](/windows/win32/Controls/tree-view-control-window-extended-styles)

Ta metoda wysyła komunikat [TVM_SETAUTOSCROLLINFO,](/windows/win32/Controls/tvm-setautoscrollinfo) który jest opisany w windows SDK.

### <a name="example"></a>Przykład

Poniższy przykład kodu definiuje `m_treeCtrl`zmienną , która jest używana do uzyskiwania dostępu do bieżącego formantu widoku drzewa. Przykład kodu definiuje również niepodpisaną całkowitej i kilka zmiennych HTREEITEM. Te zmienne są używane w następnym przykładzie.

[!code-cpp[NVC_MFC_CTreeCtrl_s1#1](../../mfc/reference/codesnippet/cpp/ctreectrl-class_17.h)]

### <a name="example"></a>Przykład

Poniższy przykład kodu ustawia zachowanie autoscroll bieżącego formantu widoku drzewa. We wcześniejszej sekcji przykładu kodu, który nie jest wyświetlany, utworzyliśmy widok drzewa, który składa się z węzła kraju/regionu głównego dla Stanów Zjednoczonych, węzłów podrzędnych dla stanów Pensylwania i Waszyngton oraz elementów drzewa dla miast w tych stanach. Celowo ustawiliśmy formant widoku drzewa zawężony, dzięki czemu musi on automatycznie przewijać, aby wyświetlić element drzewa, który ma fokus. Przykładowy kod ustawia formant widoku drzewa, aby automatycznie przewijać 30 pikseli na sekundę co 5 sekund, aż element drzewa jest w widoku.

[!code-cpp[NVC_MFC_CTreeCtrl_s1#4](../../mfc/reference/codesnippet/cpp/ctreectrl-class_33.cpp)]

## <a name="ctreectrlsetbkcolor"></a><a name="setbkcolor"></a>CTreeCtrl::SetBkColor

Ta funkcja elementu członkowskiego implementuje zachowanie [TVM_SETBKCOLOR](/windows/win32/Controls/tvm-setbkcolor)komunikatu Win32, zgodnie z opisem w windows SDK.

```
COLORREF SetBkColor(COLORREF clr);
```

### <a name="parameters"></a>Parametry

*Clr*<br/>
Wartość COLORREF zawierająca nowy kolor tła. Jeśli ta wartość wynosi -1, formant powróci do używania koloru systemowego dla koloru tła.

### <a name="return-value"></a>Wartość zwracana

Wartość COLORREF reprezentująca bieżący kolor tekstu. Jeśli ta wartość wynosi -1, formant używa koloru systemowego dla koloru tekstu.

### <a name="example"></a>Przykład

  Zobacz przykład [CTreeCtrl::SetTextColor](#settextcolor).

## <a name="ctreectrlsetcheck"></a><a name="setcheck"></a>CTreeCtrl::SetCheck

Wywołanie tej funkcji elementu członkowskiego, aby ustawić stan wyboru elementu formantu drzewa.

```
BOOL SetCheck(
    HTREEITEM hItem,
    BOOL fCheck = TRUE);
```

### <a name="parameters"></a>Parametry

*hItem (własówce)*<br/>
HTREEITEM, aby otrzymać zmianę stanu kontroli.

*fSprawda*<br/>
Wskazuje, czy element formantu drzewa ma być zaznaczony, czy niesprawdzony. Domyślnie `SetCheck` ustawia element, który ma być sprawdzany.

### <a name="return-value"></a>Wartość zwracana

Nonzero jeśli się powiedzie; w przeciwnym razie 0.

### <a name="remarks"></a>Uwagi

Gdy element formantu drzewa jest zaznaczony *(fWycesja* ustawiona na WARTOŚĆ PRAWDA) element jest wyświetlany z sąsiednim znacznikiem wyboru.

### <a name="example"></a>Przykład

[!code-cpp[NVC_MFC_CTreeCtrl#29](../../mfc/reference/codesnippet/cpp/ctreectrl-class_34.cpp)]

### <a name="example"></a>Przykład

Aby użyć pól wyboru, należy ustawić TVS_CHECKBOXES przed zapełnieniem formantu drzewa.

[!code-cpp[NVC_MFC_CTreeCtrl#30](../../mfc/reference/codesnippet/cpp/ctreectrl-class_35.cpp)]

## <a name="ctreectrlsetextendedstyle"></a><a name="setextendedstyle"></a>CTreeCtrl::SetExtendedStyle

Ustawia rozszerzone style dla bieżącego formantu widoku drzewa.

```
DWORD SetExtendedStyle(
    DWORD dwExMask,
    DWORD dwExStyles);
```

### <a name="parameters"></a>Parametry

|Parametr|Opis|
|---------------|-----------------|
|*dwExMask (maseczka dwEx)*|[w] Maska bitowa określająca, na które style w bieżącym formancie widoku drzewa ma wpływ ta metoda. Jeśli ten parametr jest zerowy, jest ignorowany, a wartość parametru *dwExStyles* jest przypisana do formantu widoku drzewa.<br /><br /> Określ kombinację zero lub bitową (OR) stylów opisanych w [stylach rozszerzonych z widokiem drzewa](/windows/win32/Controls/tree-view-control-window-extended-styles).|
|*dwExStyles (Np.*|[w] Maska bitowa określająca style w bieżącym formancie widoku drzewa do ustawionego lub wyczyszczenie.<br /><br /> Aby ustawić kombinację stylów, należy określić kombinację bitową (OR) stylów opisanych w [obszarze Style rozszerzone z widokiem drzewa](/windows/win32/Controls/tree-view-control-window-extended-styles). Aby wyczyścić zestaw stylów, określ zero.|

### <a name="return-value"></a>Wartość zwracana

Wartość zawierająca poprzednie style formantów rozszerzonych.

### <a name="remarks"></a>Uwagi

Ta metoda czyści style określone w parametrze *dwExMask,* a następnie ustawia style określone w parametrze *dwExStyles.* Tylko rozszerzone style, które odpowiadają bitów w *dwExMask* zmiany.

Ta metoda wysyła komunikat [TVM_SETEXTENDEDSTYLE,](/windows/win32/Controls/tvm-setextendedstyle) który jest opisany w windows SDK.

### <a name="example"></a>Przykład

Poniższy przykład kodu definiuje `m_treeCtrl`zmienną , która jest używana do uzyskiwania dostępu do bieżącego formantu widoku drzewa. Przykład kodu definiuje również niepodpisaną całkowitej i kilka zmiennych HTREEITEM. Te zmienne są używane w następnym przykładzie.

[!code-cpp[NVC_MFC_CTreeCtrl_s1#1](../../mfc/reference/codesnippet/cpp/ctreectrl-class_17.h)]

### <a name="example"></a>Przykład

Poniższy przykład kodu dodaje TVS_EX_AUTOHSCROLL styl rozszerzony do bieżącego formantu widoku drzewa. We wcześniejszej sekcji przykładu kodu, który nie jest wyświetlany, utworzyliśmy widok drzewa, który składa się z węzła kraju/regionu głównego dla Stanów Zjednoczonych, węzłów podrzędnych dla stanów Pensylwania i Waszyngton oraz elementów drzewa dla miast w tych stanach. Celowo ustawiliśmy formant widoku drzewa zawężony, dzięki czemu musi on automatycznie przewijać, aby wyświetlić element drzewa, który ma fokus.

[!code-cpp[NVC_MFC_CTreeCtrl_s1#3](../../mfc/reference/codesnippet/cpp/ctreectrl-class_36.cpp)]

## <a name="ctreectrlsetimagelist"></a><a name="setimagelist"></a>CTreeCtrl::SetImageList

Wywołanie tej funkcji, aby ustawić normalną lub stanową listę obrazów dla formantu widoku drzewa i ponownie rysować formant przy użyciu nowych obrazów.

```
CImageList* SetImageList(
    CImageList* pImageList,
    int nImageListType);
```

### <a name="parameters"></a>Parametry

*pImageList (Lista pImage)*<br/>
Wskaźnik do listy obrazów do przypisania. Jeśli *pImageList* ma wartość NULL, wszystkie obrazy są usuwane z formantu widoku drzewa.

*nImageListType (Typ listy)*<br/>
Typ listy obrazów do skonfigurowania. Lista obrazów może być jedną z następujących wartości:

- TVSIL_NORMAL Ustawia normalną listę obrazów, która zawiera wybrane i nieselektyjowane obrazy dla elementu widoku drzewa. Ten stan należy używać do nakładania obrazów.

- TVSIL_STATE Ustawia listę obrazów stanu, która zawiera obrazy elementów widoku drzewa, które są w stanie zdefiniowanym przez użytkownika.

### <a name="return-value"></a>Wartość zwracana

Wskaźnik do poprzedniej listy obrazów, jeśli istnieje; w przeciwnym razie NULL.

### <a name="example"></a>Przykład

  Zobacz przykład [CTreeCtrl::GetImageList](#getimagelist).

## <a name="ctreectrlsetindent"></a><a name="setindent"></a>CTreeCtrl::SetIndent

Wywołanie tej funkcji, aby ustawić szerokość wcięcie dla formantu widoku drzewa i przerysować formant, aby odzwierciedlić nową szerokość.

```
void SetIndent(UINT nIndent);
```

### <a name="parameters"></a>Parametry

*nIndent (wcięcie)*<br/>
Szerokość w pikselach wcięcie. Jeśli *nIndent* jest mniejsza niż minimalna szerokość zdefiniowana przez system, nowa szerokość jest ustawiona na minimum zdefiniowane przez system.

### <a name="example"></a>Przykład

  Zobacz przykład [CTreeCtrl::GetIndent](#getindent).

## <a name="ctreectrlsetinsertmark"></a><a name="setinsertmark"></a>CTreeCtrl::SetInsertMark

Ta funkcja elementu członkowskiego implementuje zachowanie [TVM_SETINSERTMARK](/windows/win32/Controls/tvm-setinsertmark)komunikatu Win32, zgodnie z opisem w windows SDK.

```
BOOL SetInsertMark(
    HTREEITEM hItem,
    BOOL fAfter = TRUE);
```

### <a name="parameters"></a>Parametry

*hItem (własówce)*<br/>
HTREEITEM, który określa, w którym elemencie zostanie umieszczony znak wstawiania. Jeśli ten argument ma wartość NULL, znacznik wstawiania zostanie usunięty.

*fPo*<br/>
Wartość BOOL określająca, czy znacznik wstawiania jest umieszczany przed lub po określonym elemencie. Jeśli ten argument jest niezerowy, znacznik wstawiania zostanie umieszczony po elemencie. Jeśli ten argument wynosi zero, znak wstawiania zostanie umieszczony przed zapasem.

### <a name="return-value"></a>Wartość zwracana

Nonzero jeśli się powiedzie; w przeciwnym razie 0.

### <a name="example"></a>Przykład

[!code-cpp[NVC_MFC_CTreeCtrl#31](../../mfc/reference/codesnippet/cpp/ctreectrl-class_37.cpp)]

## <a name="ctreectrlsetinsertmarkcolor"></a><a name="setinsertmarkcolor"></a>CTreeCtrl::SetInsertMarkColor

Ta funkcja elementu członkowskiego implementuje zachowanie [TVM_SETINSERTMARKCOLOR](/windows/win32/Controls/tvm-setinsertmarkcolor)komunikatu Win32, zgodnie z opisem w windows SDK.

```
COLORREF SetInsertMarkColor(COLORREF clrNew);
```

### <a name="parameters"></a>Parametry

*clrNowy*<br/>
Wartość COLORREF zawierająca nowy kolor znacznika wstawiania.

### <a name="return-value"></a>Wartość zwracana

Wartość COLORREF zawierająca poprzedni kolor znacznika wstawiania.

### <a name="example"></a>Przykład

  Zobacz przykład [CTreeCtrl::GetInsertMarkColor](#getinsertmarkcolor).

## <a name="ctreectrlsetitem"></a><a name="setitem"></a>CTreeCtrl::SetItem

Wywołanie tej funkcji, aby ustawić atrybuty określonego elementu widoku drzewa.

```
BOOL SetItem(TVITEM* pItem);

BOOL SetItem(
    HTREEITEM hItem,
    UINT nMask,
    LPCTSTR lpszItem,
    int nImage,
    int nSelectedImage,
    UINT nState,
    UINT nStateMask,
    LPARAM lParam);
```

### <a name="parameters"></a>Parametry

*pItem (własówce)*<br/>
Wskaźnik do struktury [TVITEM,](/windows/win32/api/commctrl/ns-commctrl-tvitemw) który zawiera nowe atrybuty elementu, zgodnie z opisem w windows SDK.

*hItem (własówce)*<br/>
Dojście elementu, którego atrybuty mają być ustawione. Zobacz `hItem` element członkowski `TVITEM` struktury w windows SDK.

*nMask*<br/>
Integer określający atrybuty, które mają być ustawione. Zobacz `mask` element członkowski `TVITEM` struktury.

*lpszItem*<br/>
Adres ciągu zawierającego tekst elementu.

*nImage (Obraz)*<br/>
Indeks obrazu elementu na liście obrazów formantu widoku drzewa. Zobacz `iImage` element członkowski `TVITEM` struktury.

*nWybranyImage*<br/>
Indeks wybranego obrazu elementu na liście obrazów formantu widoku drzewa. Zobacz `iSelectedImage` element członkowski `TVITEM` struktury.

*nPaństwo*<br/>
Określa wartości stanów elementu. Zobacz `State` element członkowski `TVITEM` struktury.

*nMask stanowa*<br/>
Określa, które stany mają być ustawione. Zobacz `stateMask` element członkowski `TVITEM` struktury.

*Lparam*<br/>
Wartość specyficzne dla aplikacji o rozmiarze wskaźnika skojarzona z elementem.

### <a name="return-value"></a>Wartość zwracana

Nonzero jeśli się powiedzie; w przeciwnym razie 0.

### <a name="remarks"></a>Uwagi

W `TVITEM` strukturze element `hItem` członkowski identyfikuje element, `mask` a element członkowski określa, które atrybuty mają być ustawione.

Jeśli `mask` element członkowski lub parametr *nMask* określa wartość `pszText` TVIF_TEXT, element członkowski lub *lpszItem* jest adresem `cchTextMax` ciągu zakończonego zerem, a element członkowski jest ignorowany. Jeśli `mask` (lub *nMask)* określa wartość TVIF_STATE, `stateMask` element członkowski lub parametr *nStateMask* określa, `state` które stany elementu należy zmienić, a parametr element członkowski lub *nState* zawiera wartości dla tych stanów.

### <a name="example"></a>Przykład

[!code-cpp[NVC_MFC_CTreeCtrl#32](../../mfc/reference/codesnippet/cpp/ctreectrl-class_38.cpp)]

## <a name="ctreectrlsetitemdata"></a><a name="setitemdata"></a>CTreeCtrl::SetItemData

Wywołanie tej funkcji, aby ustawić wartość specyficzne dla aplikacji skojarzone z określonym elementem.

```
BOOL SetItemData(
    HTREEITEM hItem,
    DWORD_PTR dwData);
```

### <a name="parameters"></a>Parametry

*hItem (własówce)*<br/>
Obsługa elementu, którego dane mają zostać pobrane.

*dwData (dane)*<br/>
Wartość specyficzne dla aplikacji o rozmiarze wskaźnika skojarzona z elementem określonym przez *hItem*.

### <a name="return-value"></a>Wartość zwracana

Nonzero jeśli się powiedzie; w przeciwnym razie 0.

### <a name="example"></a>Przykład

[!code-cpp[NVC_MFC_CTreeCtrl#33](../../mfc/reference/codesnippet/cpp/ctreectrl-class_39.cpp)]

## <a name="ctreectrlsetitemexpandedimageindex"></a><a name="setitemexpandedimageindex"></a>CTreeCtrl::SetItemRozwińImedindex

Ustawia indeks obrazu do wyświetlenia, gdy określony element bieżącego formantu widoku drzewa jest w stanie rozwiniętym.

```
BOOL SetItemExpandedImageIndex(
    HTREEITEM hItem,
    int iExpandedImage);
```

### <a name="parameters"></a>Parametry

|Parametr|Opis|
|---------------|-----------------|
|*hItem (własówce)*|[w] Dojście do elementu sterującego widoku drzewa.|
|*iRozwińImage*|[w] Indeks obrazu do wyświetlenia, gdy określony element jest w stanie rozwiniętym.|

### <a name="return-value"></a>Wartość zwracana

PRAWDA, jeśli ta metoda zakończy się pomyślnie; w przeciwnym razie FALSE.

### <a name="remarks"></a>Uwagi

Ta metoda wysyła komunikat [TVM_SETITEM,](/windows/win32/Controls/tvm-setitem) który jest opisany w windows SDK. Ta metoda przypisuje *iExpandedImage* parametr `iExpandedImage` do elementu członkowskiego struktury [TVITEMEX,](/windows/win32/api/commctrl/ns-commctrl-tvitemexw) a następnie używa tej struktury w wiadomości.

### <a name="example"></a>Przykład

Poniższy przykład kodu definiuje `m_treeCtrl`zmienną , która jest używana do uzyskiwania dostępu do bieżącego formantu widoku drzewa. Przykład kodu definiuje również niepodpisaną całkowitej i kilka zmiennych HTREEITEM. Te zmienne są używane w następnym przykładzie.

[!code-cpp[NVC_MFC_CTreeCtrl_s1#1](../../mfc/reference/codesnippet/cpp/ctreectrl-class_17.h)]

### <a name="example"></a>Przykład

Poniższy przykład kodu jest trywialnym testem, aby ustalić, czy metoda [CTreeCtrl::GetItemExEx](#getitemexpandedimageindex) zwraca wartość ustawioną przez metodę [CTreeCtrl::SetItemExpandedImdex.](#setitemexpandedimageindex) We wcześniejszej sekcji przykładu kodu, który nie jest wyświetlany, utworzyliśmy widok drzewa, który składa się z węzła kraju/regionu głównego dla Stanów Zjednoczonych, węzłów podrzędnych dla stanów Pensylwania i Waszyngton oraz elementów drzewa dla miast w tych stanach.

[!code-cpp[NVC_MFC_CTreeCtrl_s1#8](../../mfc/reference/codesnippet/cpp/ctreectrl-class_40.cpp)]

## <a name="ctreectrlsetitemheight"></a><a name="setitemheight"></a>CTreeCtrl::SetItemHeight

Ta funkcja elementu członkowskiego implementuje zachowanie [TVM_SETITEMHEIGHT](/windows/win32/Controls/tvm-setitemheight)komunikatu Win32, zgodnie z opisem w windows SDK.

```
SHORT SetItemHeight(SHORT cyHeight);
```

### <a name="parameters"></a>Parametry

*cyHeight (cyHeight)*<br/>
Określa nową wysokość każdego elementu w widoku drzewa w pikselach. Jeśli ten argument jest mniejszy niż wysokość obrazów, zostanie on ustawiony na wysokość obrazów. Jeśli ten argument nie jest nawet, zostanie zaokrąglona w dół do najbliższej wartości parzyste. Jeśli ten argument jest -1, formant powróci do używania jego domyślnej wysokości elementu.

### <a name="return-value"></a>Wartość zwracana

Poprzednia wysokość elementów w pikselach.

### <a name="example"></a>Przykład

  Zobacz przykład [CTreeCtrl::GetItemHeight](#getitemheight).

## <a name="ctreectrlsetitemimage"></a><a name="setitemimage"></a>CTreeCtrl::SetItemImage

Kojarzy obrazy z elementem.

```
BOOL SetItemImage(
    HTREEITEM hItem,
    int nImage,
    int nSelectedImage);
```

### <a name="parameters"></a>Parametry

*hItem (własówce)*<br/>
Dojście elementu, którego obraz ma być ustawiony.

*nImage (Obraz)*<br/>
Indeks obrazu elementu na liście obrazów formantu widoku drzewa.

*nWybranyImage*<br/>
Indeks wybranego obrazu elementu na liście obrazów formantu widoku drzewa.

### <a name="return-value"></a>Wartość zwracana

Nonzero jeśli się powiedzie; w przeciwnym razie 0.

### <a name="remarks"></a>Uwagi

Każdy element w formancie widoku drzewa może mieć parę bitmapowych obrazów skojarzonych z nim. Obrazy są wyświetlane po lewej stronie etykiety elementu. Jeden obraz jest wyświetlany po wybraniu elementu, a drugi jest wyświetlany, gdy element nie jest zaznaczony. Na przykład element może wyświetlać otwarty folder po zaznaczeniu i zamknięty folder, gdy nie jest zaznaczony.

Wywołanie tej funkcji, aby ustawić indeks obrazu elementu i jego wybranego obrazu w liście obrazów formantu widoku drzewa.

Aby uzyskać więcej informacji na temat obrazów, zobacz [CImageList](../../mfc/reference/cimagelist-class.md).

### <a name="example"></a>Przykład

  Zobacz przykład [CTreeCtrl::GetItemImage](#getitemimage).

## <a name="ctreectrlsetitemstate"></a><a name="setitemstate"></a>CTreeCtrl::Stan zestawu

Ustawia stan elementu określonego przez *hItem*.

```
BOOL SetItemState(
    HTREEITEM hItem,
    UINT nState,
    UINT nStateMask);
```

### <a name="parameters"></a>Parametry

*hItem (własówce)*<br/>
Dojście towaru, którego stan ma być ustawiony.

*nPaństwo*<br/>
Określa nowe stany dla elementu.

*nMask stanowa*<br/>
Określa, które stany mają zostać zmienione.

### <a name="return-value"></a>Wartość zwracana

Nonzero jeśli się powiedzie; w przeciwnym razie 0.

### <a name="remarks"></a>Uwagi

Aby uzyskać informacje o stanach, zobacz [CTreeCtrl::GetItem](#getitem).

### <a name="example"></a>Przykład

  Zobacz przykład [CTreeCtrl::GetItemState](#getitemstate).

## <a name="ctreectrlsetitemstateex"></a><a name="setitemstateex"></a>CTreeCtrl::SetItemStateEx

Ustawia rozszerzony stan określonego elementu w bieżącym formancie widoku drzewa.

```
BOOL SetItemStateEx(
    HTREEITEM hItem,
    UINT uStateEx);
```

### <a name="parameters"></a>Parametry

|Parametr|Opis|
|---------------|-----------------|
|*hItem (własówce)*|[w] Dojście do elementu sterującego widoku drzewa.|
|*uStateEx*|[w] Rozszerzony stan elementu. Aby uzyskać więcej `uStateEx` informacji, zobacz element struktury [TVITEMEX.](/windows/win32/api/commctrl/ns-commctrl-tvitemexw)|

### <a name="return-value"></a>Wartość zwracana

PRAWDA, jeśli ta metoda zakończy się pomyślnie; w przeciwnym razie FALSE.

### <a name="remarks"></a>Uwagi

Ta metoda wysyła komunikat [TVM_SETITEM,](/windows/win32/Controls/tvm-setitem) który jest opisany w windows SDK. Ta metoda przypisuje parametr *uStateEx* do `uStateEx` elementu członkowskiego struktury [TVITEMEX,](/windows/win32/api/commctrl/ns-commctrl-tvitemexw) a następnie używa tej struktury w komunikacie.

### <a name="example"></a>Przykład

Poniższy przykład kodu definiuje `m_treeCtrl`zmienną , która jest używana do uzyskiwania dostępu do bieżącego formantu widoku drzewa. Przykład kodu definiuje również niepodpisaną całkowitej i kilka zmiennych HTREEITEM. Te zmienne są używane w następnym przykładzie.

[!code-cpp[NVC_MFC_CTreeCtrl_s1#1](../../mfc/reference/codesnippet/cpp/ctreectrl-class_17.h)]

### <a name="example"></a>Przykład

Poniższy przykład kodu ustawia element widoku drzewa do stanu wyłączonego. We wcześniejszej sekcji przykładu kodu, który nie jest wyświetlany, utworzyliśmy widok drzewa, który składa się z węzła kraju/regionu głównego dla Stanów Zjednoczonych, węzłów podrzędnych dla stanów Pensylwania i Waszyngton oraz elementów drzewa dla miast w tych stanach. W tym przykładzie kodu ustawia węzeł Pensylwania do stanu wyłączonego.

[!code-cpp[NVC_MFC_CTreeCtrl_s1#7](../../mfc/reference/codesnippet/cpp/ctreectrl-class_41.cpp)]

## <a name="ctreectrlsetitemtext"></a><a name="setitemtext"></a>CTreeCtrl::SetItemText

Ustawia tekst elementu określonego przez *hItem*.

```
BOOL SetItemText(
    HTREEITEM hItem,
    LPCTSTR lpszItem);
```

### <a name="parameters"></a>Parametry

*hItem (własówce)*<br/>
Dojście elementu, którego tekst ma być ustawiony.

*lpszItem*<br/>
Adres ciągu zawierającego nowy tekst elementu

### <a name="return-value"></a>Wartość zwracana

Nonzero jeśli się powiedzie; w przeciwnym razie 0.

### <a name="example"></a>Przykład

[!code-cpp[NVC_MFC_CTreeCtrl#34](../../mfc/reference/codesnippet/cpp/ctreectrl-class_42.cpp)]

## <a name="ctreectrlsetlinecolor"></a><a name="setlinecolor"></a>CTreeCtrl::SetLineColor

Wywołanie tej funkcji elementu członkowskiego, aby ustawić bieżący kolor linii dla formantu widoku drzewa.

```
COLORREF SetLineColor(COLORREF clrNew = CLR_DEFAULT);
```

### <a name="parameters"></a>Parametry

*clrNowy*<br/>
Nowy kolor linii.

### <a name="return-value"></a>Wartość zwracana

Poprzedni kolor linii.

### <a name="remarks"></a>Uwagi

Ta funkcja elementu członkowskiego implementuje zachowanie [TVM_SETLINECOLOR](/windows/win32/Controls/tvm-setlinecolor)komunikatu Win32, zgodnie z opisem w windows SDK.

### <a name="example"></a>Przykład

[!code-cpp[NVC_MFC_CTreeCtrl#35](../../mfc/reference/codesnippet/cpp/ctreectrl-class_43.cpp)]

## <a name="ctreectrlsetscrolltime"></a><a name="setscrolltime"></a>CTreeCtrl::SetScrollTime

Wywołanie tej funkcji elementu członkowskiego, aby ustawić maksymalny czas przewijania dla formantu widoku drzewa.

```
UINT SetScrollTime(UINT uScrollTime);
```

### <a name="parameters"></a>Parametry

*uScrollTime*<br/>
Nowy maksymalny czas przewijania w milisekundach. Jeśli ta wartość jest mniejsza niż 100, zostanie zaokrąglona do 100.

### <a name="return-value"></a>Wartość zwracana

Poprzedni maksymalny czas przewijania w milisekundach.

### <a name="remarks"></a>Uwagi

Ta funkcja elementu członkowskiego implementuje zachowanie [TVM_SETSCROLLTIME](/windows/win32/Controls/tvm-setscrolltime)komunikatu Win32, zgodnie z opisem w windows SDK.

## <a name="ctreectrlsettextcolor"></a><a name="settextcolor"></a>CTreeCtrl::SetTextColor

Ta funkcja elementu członkowskiego implementuje zachowanie [TVM_SETTEXTCOLOR](/windows/win32/Controls/tvm-settextcolor)komunikatu Win32, zgodnie z opisem w windows SDK.

```
COLORREF SetTextColor(COLORREF clr);
```

### <a name="parameters"></a>Parametry

*Clr*<br/>
Wartość COLORREF zawierająca nowy kolor tekstu. Jeśli ten argument jest -1, formant powróci do używania koloru systemowego dla koloru tekstu.

### <a name="return-value"></a>Wartość zwracana

Wartość COLORREF reprezentująca poprzedni kolor tekstu. Jeśli ta wartość wynosi -1, formant używał koloru systemowego dla koloru tekstu.

### <a name="example"></a>Przykład

[!code-cpp[NVC_MFC_CTreeCtrl#36](../../mfc/reference/codesnippet/cpp/ctreectrl-class_44.cpp)]

## <a name="ctreectrlsettooltips"></a><a name="settooltips"></a>CTreeCtrl::SetToolTips

Ta funkcja elementu członkowskiego implementuje zachowanie [TVM_SETTOOLTIPS](/windows/win32/Controls/tvm-settooltips)komunikatu Win32, zgodnie z opisem w windows SDK.

```
CToolTipCtrl* SetToolTips(CToolTipCtrl* pWndTip);
```

### <a name="parameters"></a>Parametry

*pWdTip*<br/>
Wskaźnik do [CToolTipCtrl](../../mfc/reference/ctooltipctrl-class.md) obiektu, który będzie używany formant drzewa.

### <a name="return-value"></a>Wartość zwracana

Wskaźnik do [CToolTipCtrl](../../mfc/reference/ctooltipctrl-class.md) obiektu zawierającego etykietki narzędzia wcześniej używane przez formant lub NULL, jeśli nie zostały wcześniej użyte żadne etykietki narzędzi.

### <a name="remarks"></a>Uwagi

Aby użyć etykietek narzędzi, podczas tworzenia `CTreeCtrl` obiektu należy wskazać styl TVS_NOTOOLTIPS.

### <a name="example"></a>Przykład

  Zobacz przykład [CTreeCtrl::GetToolTips](#gettooltips).

## <a name="ctreectrlshowinfotip"></a><a name="showinfotip"></a>CTreeCtrl::PokażTip

Wyświetla informacje dla określonego elementu w bieżącym formancie widoku drzewa.

```
void ShowInfoTip(HTREEITEM hItem);
```

### <a name="parameters"></a>Parametry

|Parametr|Opis|
|---------------|-----------------|
|*hItem (własówce)*|[w] Dojście do elementu widoku drzewa w formancie. Aby uzyskać więcej `hItem` informacji, zobacz element struktury [TVITEMEX.](/windows/win32/api/commctrl/ns-commctrl-tvitemexw)|

### <a name="remarks"></a>Uwagi

Aby uzyskać więcej informacji na temat różnicy między etykietkami narzędzi a etykietkami informacyjnymi, wyszukaj temat "Etykietki narzędzi i informacje" w [witrynie Microsoft Developer Network](https://go.microsoft.com/fwlink/p/?linkid=56322).

Ta metoda wysyła komunikat [TVM_SHOWINFOTIP,](/windows/win32/Controls/tvm-showinfotip) który jest opisany w windows SDK.

## <a name="ctreectrlsortchildren"></a><a name="sortchildren"></a>CTreeCtrl::SortChildren

Wywołanie tej funkcji alfabetycznie sortowania elementów podrzędnych danego elementu nadrzędnego w formancie widoku drzewa.

```
BOOL SortChildren(HTREEITEM hItem);
```

### <a name="parameters"></a>Parametry

*hItem (własówce)*<br/>
Uchwyt elementu nadrzędnego, którego elementy podrzędne mają być sortowane. Jeśli *hItem* ma wartość NULL, sortowanie będzie kontynuowane z katalogu głównego drzewa.

### <a name="return-value"></a>Wartość zwracana

Nonzero jeśli się powiedzie; w przeciwnym razie 0.

### <a name="remarks"></a>Uwagi

`SortChildren`nie będzie się powtarzać przez drzewo; tylko bezpośrednie dzieci *hItem* zostaną posortowane.

### <a name="example"></a>Przykład

[!code-cpp[NVC_MFC_CTreeCtrl#37](../../mfc/reference/codesnippet/cpp/ctreectrl-class_45.cpp)]

## <a name="ctreectrlsortchildrencb"></a><a name="sortchildrencb"></a>CTreeCtrl::SortChildrenCB

Wywołanie tej funkcji, aby sortować elementy widoku drzewa przy użyciu funkcji wywołania zwrotnego zdefiniowanej przez aplikację, która porównuje elementy.

```
BOOL SortChildrenCB(LPTVSORTCB pSort);
```

### <a name="parameters"></a>Parametry

*pSort*<br/>
Wskaźnik do struktury [TVSORTCB.](/windows/win32/api/commctrl/ns-commctrl-tvsortcb)

### <a name="return-value"></a>Wartość zwracana

Nonzero jeśli się powiedzie; w przeciwnym razie 0.

### <a name="remarks"></a>Uwagi

Funkcja porównania struktury, `lpfnCompare`musi zwracać wartość ujemną, jeśli pierwszy element powinien poprzedzać drugi, wartość dodatnia, jeśli pierwszy element powinien podążać za drugim lub zero, jeśli dwa elementy są równoważne.

Parametry *lParam1* i *lParam2* `lParam` odpowiadają elementowi członkowskiemu struktury [TVITEM](/windows/win32/api/commctrl/ns-commctrl-tvitemw) dla dwóch porównywanych elementów. Parametr *lParamSort* odpowiada elementowi `lParam` członkowskiemu `TV_SORTCB` struktury.

### <a name="example"></a>Przykład

[!code-cpp[NVC_MFC_CTreeCtrl#38](../../mfc/reference/codesnippet/cpp/ctreectrl-class_46.cpp)]

[!code-cpp[NVC_MFC_CTreeCtrl#39](../../mfc/reference/codesnippet/cpp/ctreectrl-class_47.cpp)]

## <a name="see-also"></a>Zobacz też

[Próbka MFC CMNCTRL1](../../overview/visual-cpp-samples.md)<br/>
[Klasa CWnd](../../mfc/reference/cwnd-class.md)<br/>
[Wykres hierarchii](../../mfc/hierarchy-chart.md)<br/>
[Klasa CImageList](../../mfc/reference/cimagelist-class.md)
