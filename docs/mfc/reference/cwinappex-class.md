---
title: Klasa CWinAppEx
ms.date: 11/04/2016
f1_keywords:
- CWinAppEx
- AFXWINAPPEX/CWinAppEx
- AFXWINAPPEX/CWinAppEx::CWinAppEx
- AFXWINAPPEX/CWinAppEx::CleanState
- AFXWINAPPEX/CWinAppEx::EnableLoadWindowPlacement
- AFXWINAPPEX/CWinAppEx::EnableTearOffMenus
- AFXWINAPPEX/CWinAppEx::EnableUserTools
- AFXWINAPPEX/CWinAppEx::ExitInstance
- AFXWINAPPEX/CWinAppEx::GetBinary
- AFXWINAPPEX/CWinAppEx::GetContextMenuManager
- AFXWINAPPEX/CWinAppEx::GetDataVersion
- AFXWINAPPEX/CWinAppEx::GetDataVersionMajor
- AFXWINAPPEX/CWinAppEx::GetDataVersionMinor
- AFXWINAPPEX/CWinAppEx::GetInt
- AFXWINAPPEX/CWinAppEx::GetKeyboardManager
- AFXWINAPPEX/CWinAppEx::GetMouseManager
- AFXWINAPPEX/CWinAppEx::GetObject
- AFXWINAPPEX/CWinAppEx::GetRegSectionPath
- AFXWINAPPEX/CWinAppEx::GetRegistryBase
- AFXWINAPPEX/CWinAppEx::GetSectionBinary
- AFXWINAPPEX/CWinAppEx::GetSectionInt
- AFXWINAPPEX/CWinAppEx::GetSectionObject
- AFXWINAPPEX/CWinAppEx::GetSectionString
- AFXWINAPPEX/CWinAppEx::GetShellManager
- AFXWINAPPEX/CWinAppEx::GetString
- AFXWINAPPEX/CWinAppEx::GetTooltipManager
- AFXWINAPPEX/CWinAppEx::GetUserToolsManager
- AFXWINAPPEX/CWinAppEx::InitContextMenuManager
- AFXWINAPPEX/CWinAppEx::InitKeyboardManager
- AFXWINAPPEX/CWinAppEx::InitMouseManager
- AFXWINAPPEX/CWinAppEx::InitShellManager
- AFXWINAPPEX/CWinAppEx::InitTooltipManager
- AFXWINAPPEX/CWinAppEx::IsResourceSmartUpdate
- AFXWINAPPEX/CWinAppEx::IsStateExists
- AFXWINAPPEX/CWinAppEx::LoadState
- AFXWINAPPEX/CWinAppEx::OnAppContextHelp
- AFXWINAPPEX/CWinAppEx::OnViewDoubleClick
- AFXWINAPPEX/CWinAppEx::OnWorkspaceIdle
- AFXWINAPPEX/CWinAppEx::SaveState
- AFXWINAPPEX/CWinAppEx::SetRegistryBase
- AFXWINAPPEX/CWinAppEx::ShowPopupMenu
- AFXWINAPPEX/CWinAppEx::WriteBinary
- AFXWINAPPEX/CWinAppEx::WriteInt
- AFXWINAPPEX/CWinAppEx::WriteObject
- AFXWINAPPEX/CWinAppEx::WriteSectionBinary
- AFXWINAPPEX/CWinAppEx::WriteSectionInt
- AFXWINAPPEX/CWinAppEx::WriteSectionObject
- AFXWINAPPEX/CWinAppEx::WriteSectionString
- AFXWINAPPEX/CWinAppEx::WriteString
- AFXWINAPPEX/CWinAppEx::LoadCustomState
- AFXWINAPPEX/CWinAppEx::LoadWindowPlacement
- AFXWINAPPEX/CWinAppEx::OnClosingMainFrame
- AFXWINAPPEX/CWinAppEx::PreLoadState
- AFXWINAPPEX/CWinAppEx::PreSaveState
- AFXWINAPPEX/CWinAppEx::ReloadWindowPlacement
- AFXWINAPPEX/CWinAppEx::SaveCustomState
- AFXWINAPPEX/CWinAppEx::StoreWindowPlacement
- AFXWINAPPEX/CWinAppEx::m_bForceImageReset
helpviewer_keywords:
- CWinAppEx [MFC], CWinAppEx
- CWinAppEx [MFC], CleanState
- CWinAppEx [MFC], EnableLoadWindowPlacement
- CWinAppEx [MFC], EnableTearOffMenus
- CWinAppEx [MFC], EnableUserTools
- CWinAppEx [MFC], ExitInstance
- CWinAppEx [MFC], GetBinary
- CWinAppEx [MFC], GetContextMenuManager
- CWinAppEx [MFC], GetDataVersion
- CWinAppEx [MFC], GetDataVersionMajor
- CWinAppEx [MFC], GetDataVersionMinor
- CWinAppEx [MFC], GetInt
- CWinAppEx [MFC], GetKeyboardManager
- CWinAppEx [MFC], GetMouseManager
- CWinAppEx [MFC], GetObject
- CWinAppEx [MFC], GetRegSectionPath
- CWinAppEx [MFC], GetRegistryBase
- CWinAppEx [MFC], GetSectionBinary
- CWinAppEx [MFC], GetSectionInt
- CWinAppEx [MFC], GetSectionObject
- CWinAppEx [MFC], GetSectionString
- CWinAppEx [MFC], GetShellManager
- CWinAppEx [MFC], GetString
- CWinAppEx [MFC], GetTooltipManager
- CWinAppEx [MFC], GetUserToolsManager
- CWinAppEx [MFC], InitContextMenuManager
- CWinAppEx [MFC], InitKeyboardManager
- CWinAppEx [MFC], InitMouseManager
- CWinAppEx [MFC], InitShellManager
- CWinAppEx [MFC], InitTooltipManager
- CWinAppEx [MFC], IsResourceSmartUpdate
- CWinAppEx [MFC], IsStateExists
- CWinAppEx [MFC], LoadState
- CWinAppEx [MFC], OnAppContextHelp
- CWinAppEx [MFC], OnViewDoubleClick
- CWinAppEx [MFC], OnWorkspaceIdle
- CWinAppEx [MFC], SaveState
- CWinAppEx [MFC], SetRegistryBase
- CWinAppEx [MFC], ShowPopupMenu
- CWinAppEx [MFC], WriteBinary
- CWinAppEx [MFC], WriteInt
- CWinAppEx [MFC], WriteObject
- CWinAppEx [MFC], WriteSectionBinary
- CWinAppEx [MFC], WriteSectionInt
- CWinAppEx [MFC], WriteSectionObject
- CWinAppEx [MFC], WriteSectionString
- CWinAppEx [MFC], WriteString
- CWinAppEx [MFC], LoadCustomState
- CWinAppEx [MFC], LoadWindowPlacement
- CWinAppEx [MFC], OnClosingMainFrame
- CWinAppEx [MFC], PreLoadState
- CWinAppEx [MFC], PreSaveState
- CWinAppEx [MFC], ReloadWindowPlacement
- CWinAppEx [MFC], SaveCustomState
- CWinAppEx [MFC], StoreWindowPlacement
- CWinAppEx [MFC], m_bForceImageReset
ms.assetid: a3d3e053-3e22-463f-9444-c73abb1bb9d7
ms.openlocfilehash: 1e022f09986f5d49ab2bba44c62cb716ce939a0c
ms.sourcegitcommit: c123cc76bb2b6c5cde6f4c425ece420ac733bf70
ms.translationtype: MT
ms.contentlocale: pl-PL
ms.lasthandoff: 04/14/2020
ms.locfileid: "81369613"
---
# <a name="cwinappex-class"></a>Klasa CWinAppEx

`CWinAppEx`obsługuje stan aplikacji, zapisuje stan w rejestrze, ładuje stan z rejestru, inicjuje menedżerów aplikacji i udostępnia łącza do tych samych menedżerów aplikacji.

Aby uzyskać więcej informacji, zobacz kod źródłowy znajdujący się w folderze **vc\\\\atlmfc src\\mfc** instalacji programu Visual Studio.

## <a name="syntax"></a>Składnia

```
class CWinAppEx : public CWinApp
```

## <a name="members"></a>Elementy członkowskie

### <a name="public-constructors"></a>Konstruktory publiczne

|Nazwa|Opis|
|----------|-----------------|
|[CWinAppEx::CWinAppEx](#cwinappex)|Konstruuje `CWinAppEx` obiekt.|

### <a name="public-methods"></a>Metody publiczne

|Nazwa|Opis|
|----------|-----------------|
|[CWinAppEx::CleanState](#cleanstate)|Usuwa informacje o aplikacji z rejestru systemu Windows.|
|[CWinAppEx::EnableLoadWindowPlacement](#enableloadwindowplacement)|Określa, czy aplikacja załaduje początkowy rozmiar i lokalizację okna ramki głównej z rejestru.|
|[CWinAppEx::EnableTearOffMenus](#enabletearoffmenus)|Włącza odrywające menu dla aplikacji.|
|[Cwinappex::enableusertools](#enableusertools)|Umożliwia użytkownikowi tworzenie niestandardowych poleceń menu w aplikacji.|
|[CWinAppEx::Zakończ nienasyk](#exitinstance)|Wywoływane przez strukturę `Run` z wewnątrz funkcji elementu członkowskiego, aby zakończyć to wystąpienie aplikacji. (Zastępuje [CWinApp::ExitInstance](../../mfc/reference/cwinapp-class.md#exitinstance).)|
|[CWinAppEx::GetBinary](#getbinary)|Odczytuje dane binarne skojarzone z określoną wartością rejestru.|
|[CWinAppEx::GetContextMenuManager](#getcontextmenumanager)|Zwraca wskaźnik do globalnego [obiektu CContextMenuManager.](../../mfc/reference/ccontextmenumanager-class.md)|
|[CWinAppEx::GetDataVersion](#getdataversion)||
|[CWinAppEx::GetDataVersionMajor](#getdataversionmajor)|Zwraca główną wersję aplikacji zapisaną w rejestrze systemu Windows.|
|[CWinAppEx::GetDataVersionMinor](#getdataversionminor)|Zwraca pomocniczą wersję aplikacji zapisaną w rejestrze systemu Windows.|
|[CWinAppEx::GetInt](#getint)|Odczytuje dane liczbowe skojarzone z określoną wartością z rejestru.|
|[CWinAppEx::GetKeyboardManager](#getkeyboardmanager)|Zwraca wskaźnik do globalnego [obiektu CKeyboardManager.](../../mfc/reference/ckeyboardmanager-class.md)|
|[CWinAppEx::GetMouseManager](#getmousemanager)|Zwraca wskaźnik do globalnego [obiektu CMouseManager.](../../mfc/reference/cmousemanager-class.md)|
|[CWinAppEx::GetObject](#getobject)|Odczytuje `CObject`dane pochodne, które są skojarzone z określoną wartością z rejestru.|
|[CWinAppEx::GetRegSectionPath](#getregsectionpath)|Zwraca ciąg, który jest ścieżką klucza rejestru. Ta ścieżka łączy dostarczoną ścieżkę względną ze ścieżką aplikacji.|
|[CWinAppEx::GetRegistryBase](#getregistrybase)|Zwraca ścieżkę rejestru dla aplikacji.|
|[CWinAppEx::GetSectionBinary](#getsectionbinary)|Odczytuje dane binarne skojarzone z określonym kluczem i wartością z rejestru.|
|[CWinAppEx::GetSectionInt](#getsectionint)|Odczytuje dane liczbowe z rejestru skojarzonego z określonym kluczem i wartością.|
|[CWinAppEx::GetSectionObject](#getsectionobject)|Odczytuje `CObject` dane skojarzone z określonym kluczem i wartością z rejestru.|
|[CWinAppEx::GetSectionString](#getsectionstring)|Odczytuje dane ciągu skojarzone z określonym kluczem i wartością z rejestru.|
|[CWinAppEx::Menedżer GetShell](#getshellmanager)|Zwraca wskaźnik do globalnego [obiektu CShellManager.](../../mfc/reference/cshellmanager-class.md)|
|[CWinAppEx::GetString](#getstring)|Odczytuje dane ciągu skojarzone z określoną wartością z rejestru.|
|[CWinAppEx::GetTooltipManager](#gettooltipmanager)|Zwraca wskaźnik do globalnego [obiektu CTooltipManager.](../../mfc/reference/ctooltipmanager-class.md)|
|[CWinAppEx::GetUserToolsManager](#getusertoolsmanager)|Zwraca wskaźnik do globalnego [obiektu CUserToolsManager.](../../mfc/reference/cusertoolsmanager-class.md)|
|[CWinAppEx::InitContextMenuManager](#initcontextmenumanager)|Inicjuje `CContextMenuManager` obiekt.|
|[CWinAppEx::InitKeyboardManager](#initkeyboardmanager)|Inicjuje `CKeyboardManager` obiekt.|
|[CWinAppEx::InitMouseManager](#initmousemanager)|Inicjuje `CMouseManager` obiekt.|
|[CWinAppEx::InitShellManager](#initshellmanager)|Inicjuje `CShellManager` klasę|
|[CWinAppEx::InitTooltipManager](#inittooltipmanager)|Inicjuje `CTooltipManager` klasę.|
|[CWinAppEx::IsResourceSmartUpdate](#isresourcesmartupdate)||
|[CWinAppEx::IsStateExists](#isstateexists)|Wskazuje, czy określony klucz znajduje się w rejestrze.|
|[CWinAppEx::Stan obciążenia](#loadstate)|Ładuje stan aplikacji z rejestru.|
|[CWinAppEx::OnAppContextHelp](#onappcontexthelp)|Wywoływane przez strukturę, gdy użytkownik żąda pomocy kontekstu dla okna dialogowego **Dostosowywanie.**|
|[CWinAppEx::OnViewDoubleClick](#onviewdoubleclick)|Wywołuje polecenie zdefiniowane przez użytkownika, gdy użytkownik kliknie dwukrotnie w dowolnym miejscu w aplikacji.|
|[CWinAppEx::OnWorkspaceIdle](#onworkspaceidle)||
|[CWinAppEx::Stan zapisu](#savestate)|Zapisuje stan struktury aplikacji do rejestru systemu Windows.|
|[CWinAppEx::SetRegistryBase](#setregistrybase)|Ustawia ścieżkę domyślnego klucza rejestru. Ten klucz będzie służyć jako korzeń dla wszystkich kolejnych wywołań rejestru.|
|[CWinAppEx::ShowPopupMenu](#showpopupmenu)|Wyświetla menu podręczne.|
|[CWinAppEx::WriteBinary](#writebinary)|Zapisuje dane binarne do określonej wartości rejestru.|
|[CWinAppEx::WriteInt](#writeint)|Zapisuje dane liczbowe do określonej wartości rejestru.|
|[CWinAppEx::WriteObject](#writeobject)|Zapisuje dane, które są pochodną [klasy CObject](../../mfc/reference/cobject-class.md) do określonej wartości rejestru.|
|[CWinAppEx::WriteSectionBinary](#writesectionbinary)|Zapisuje dane binarne do wartości określonego klucza rejestru.|
|[CWinAppEx::WriteSectionInt](#writesectionint)|Zapisuje dane liczbowe do wartości określonego klucza rejestru.|
|[CWinAppEx::WriteSectionObject](#writesectionobject)|Zapisuje dane pochodzące `CObject` z klasy do wartości określonego klucza rejestru.|
|[CWinAppEx::WriteSectionString](#writesectionstring)|Zapisuje dane ciągu do wartości określonego klucza rejestru.|
|[CWinAppEx::WriteString](#writestring)|Zapisuje dane ciągu do określonej wartości rejestru.|

### <a name="protected-methods"></a>Metody chronione

|Nazwa|Opis|
|----------|-----------------|
|[CWinAppEx::LoadCustomState](#loadcustomstate)|Wywoływana przez platformę, gdy stan aplikacji został załadowany.|
|[CWinAppEx::LoadWindowPlacement](#loadwindowplacement)|Wywoływana przez platformę, gdy ładuje rozmiar i lokalizację aplikacji z rejestru. Załadowane dane obejmują rozmiar i lokalizację ramki głównej w momencie ostatniego zamknięcia aplikacji.|
|[CWinAppEx::OnClosingMainFrame](#onclosingmainframe)|Wywoływana przez strukturę, gdy okno ramki głównej przetwarza WM_CLOSE.|
|[CWinAppEx::PreLoadState](#preloadstate)|Wywoływane przez strukturę bezpośrednio przed załadowaniem stanu aplikacji.|
|[CWinAppEx::PreSaveState](#presavestate)|Wywoływane przez strukturę bezpośrednio przed stan aplikacji jest zapisywany.|
|[CWinAppEx::ReloadWindowPlacement](#reloadwindowplacement)|Ponownie ładuje rozmiar i lokalizację dostarczonego okna z rejestru|
|[CWinAppEx::Zapisz stan](#savecustomstate)|Wywoływana przez platformę po zapisuje stan aplikacji do rejestru.|
|[CWinAppEx::StoreWindowPlacement](#storewindowplacement)|Wywoływana przez platformę do zapisu rozmiaru i lokalizacji ramki głównej do rejestru.|

### <a name="data-members"></a>Elementy członkowskie danych

|Nazwa|Opis|
|----------|-----------------|
|[CWinAppEx::m_bForceImageReset](#m_bforceimagereset)|Określa, czy struktura zresetuje wszystkie obrazy paska narzędzi po załadowaniu okna ramki zawierającego pasek narzędzi.|

## <a name="remarks"></a>Uwagi

Wiele funkcji dostarczonych przez platformę MFC `CWinAppEx` zależy od klasy. Klasę można włączyć `CWinAppEx` do aplikacji na jeden z dwóch sposobów:

- Skonstruuj `CWinAppEx` klasę w wątku głównym.

- Wyprowadzić główną klasę aplikacji z `CWinAppEx`.

Po włączeniu `CWinAppEx` do aplikacji można zainicjować dowolnego z menedżerów aplikacji. Przed użyciem menedżera aplikacji należy go zainicjować, wywołując odpowiednią metodę inicjowania. Aby uzyskać wskaźnik do określonego menedżera, wywołaj skojarzoną metodę get. Klasa `CWinAppEx` zarządza następującymi menedżerami aplikacji: [CMouseManager Class](../../mfc/reference/cmousemanager-class.md), [CContextMenuManager Class](../../mfc/reference/ccontextmenumanager-class.md), [CKeyboardManager Class](../../mfc/reference/ckeyboardmanager-class.md), [CUserToolsManager Class](../../mfc/reference/cusertoolsmanager-class.md)i [CMenuTearOffManager Class](../../mfc/reference/cmenutearoffmanager-class.md).

## <a name="inheritance-hierarchy"></a>Hierarchia dziedziczenia

[Cobject](../../mfc/reference/cobject-class.md)

[Ccmdtarget](../../mfc/reference/ccmdtarget-class.md)

[Cwinthread](../../mfc/reference/cwinthread-class.md)

[Cwinapp](../../mfc/reference/cwinapp-class.md)

[Cwinappex](../../mfc/reference/cwinappex-class.md)

## <a name="requirements"></a>Wymagania

**Nagłówek:** afxwinappex.h

## <a name="cwinappexcleanstate"></a><a name="cleanstate"></a>CWinAppEx::CleanState

Usuwa wszystkie informacje o aplikacji z rejestru systemu Windows.

```
virtual BOOL CleanState(LPCTSTR lpszSectionName=NULL);
```

### <a name="parameters"></a>Parametry

*lpszSectionName*<br/>
[w] Ciąg zawierający ścieżkę klucza rejestru.

### <a name="return-value"></a>Wartość zwracana

Nonzero, jeśli metoda zakończyła się pomyślnie; w przeciwnym razie 0.

### <a name="remarks"></a>Uwagi

Ta metoda czyści dane aplikacji z określonej sekcji rejestru. Można określić sekcję, aby wyczyścić za pomocą parametru *lpszSectionName*. Jeśli *lpszSectionName* jest NULL, ta metoda użyje `CWinAppEx` domyślnej ścieżki rejestru przechowywane w obiekcie. Aby uzyskać domyślną ścieżkę rejestru, użyj [CWinAppEx::GetRegistryBase](#getregistrybase).

## <a name="cwinappexcwinappex"></a><a name="cwinappex"></a>CWinAppEx::CWinAppEx

Konstruuje `CWinAppEx` obiekt.

```
CWinAppEx(BOOL bResourceSmartUpdate = FALSE);
```

### <a name="parameters"></a>Parametry

*bSourceSmartUpdate*<br/>
[w] Parametr logiczny określający, czy obiekt obszaru roboczego powinien wykrywać i obsługiwać aktualizacje zasobów.

### <a name="remarks"></a>Uwagi

Klasa `CWinAppEx` ma metody inicjowania, zapewnia funkcje zapisywania i ładowania informacji o aplikacji do rejestru i kontroluje globalne ustawienia aplikacji. Umożliwia również korzystanie z menedżerów globalnych, takich jak [CKeyboardManager Class](../../mfc/reference/ckeyboardmanager-class.md) i [CUserToolsManager Class](../../mfc/reference/cusertoolsmanager-class.md). Każda aplikacja może mieć tylko `CWinAppEx` jedno wystąpienie klasy.

## <a name="cwinappexenableloadwindowplacement"></a><a name="enableloadwindowplacement"></a>CWinAppEx::EnableLoadWindowPlacement

Określa, czy aplikacja załaduje początkowy rozmiar i lokalizację okna ramki głównej z rejestru.

```
void EnableLoadWindowPlacement(BOOL bEnable = TRUE);
```

### <a name="parameters"></a>Parametry

*bWłaszą*<br/>
[w] Określa, czy aplikacja ładuje początkowy rozmiar i lokalizację okna ramki głównej z rejestru.

### <a name="remarks"></a>Uwagi

Domyślnie rozmiar i lokalizacja ramki głównej są ładowane z rejestru wraz z innymi ustawieniami aplikacji. Dzieje się tak podczas [CWinAppEx::LoadState](#loadstate). Jeśli nie chcesz ładować początkowego rozmieszczenia okna z rejestru, należy wywołać tę metodę z *bEnable ustawioną* na FALSE.

## <a name="cwinappexenabletearoffmenus"></a><a name="enabletearoffmenus"></a>CWinAppEx::EnableTearOffMenus

Tworzy i inicjuje [CMenuTearOffManager](../../mfc/reference/cmenutearoffmanager-class.md) obiektu.

```
BOOL EnableTearOffMenus(
    LPCTSTR lpszRegEntry,
    const UINT uiCmdFirst,
    const UINT uiCmdLast);
```

### <a name="parameters"></a>Parametry

*lpszRegEntry*<br/>
[w] Ciąg zawierający ścieżkę klucza rejestru. Aplikacja używa tego klucza rejestru do przechowywania informacji dla menu odrywać.

*uiCmdPierwsz*<br/>
[w] Pierwszy identyfikator menu oderwij.

*uiCmdLast*<br/>
[w] Identyfikator ostatniego odrywu.

### <a name="return-value"></a>Wartość zwracana

PRAWDA, `CMenuTearOffManager` jeśli zostanie pomyślnie utworzony i zainicjowany; FAŁSZ, jeśli wystąpi `CMenuTearOffManager` błąd lub jeśli już istnieje.

### <a name="remarks"></a>Uwagi

Ta funkcja służy do włączania menu odrywać w aplikacji. Tę funkcję należy `InitInstance`wywołać z pliku .

## <a name="cwinappexenableusertools"></a><a name="enableusertools"></a>Cwinappex::enableusertools

Umożliwia użytkownikowi tworzenie niestandardowych poleceń menu, które zmniejszają naciśnięcia klawiszy w aplikacji. Ta metoda tworzy [CUserToolsManager](../../mfc/reference/cusertoolsmanager-class.md) obiektu.

```
BOOL EnableUserTools(
    const UINT uiCmdToolsDummy,
    const UINT uiCmdFirst,
    const UINT uiCmdLast,
    CRuntimeClass* pToolRTC = RUNTIME_CLASS(CUserTool),
    UINT uArgMenuID = 0,
    UINT uInitDirMenuID = 0);
```

### <a name="parameters"></a>Parametry

*uiCmdToolsDummy*<br/>
[w] Niepodpisana liczba całkowita, której struktura używa jako symbolu zastępczego dla identyfikatora polecenia menu narzędzi użytkownika.

*uiCmdPierwsz*<br/>
[w] Identyfikator polecenia dla pierwszego polecenia narzędzia użytkownika.

*uiCmdLast*<br/>
[w] Identyfikator polecenia dla ostatniego polecenia narzędzia użytkownika.

*pToolRTC*<br/>
[w] Klasa używana `CUserToolsManager` przez obiekt do tworzenia nowych narzędzi użytkownika.

*uArgMenuID*<br/>
[w] Identyfikator menu argumentów.

*uInitDirMenuID*<br/>
[w] Identyfikator menu dla początkowego katalogu narzędzi.

### <a name="return-value"></a>Wartość zwracana

PRAWDA, jeśli metoda tworzy i `CUserToolsManager` inicjuje obiekt; FAŁSZ, jeśli metoda `CUserToolsManager` nie powiedzie się lub jeśli obiekt już istnieje.

### <a name="remarks"></a>Uwagi

Po włączeniu narzędzi zdefiniowanych przez użytkownika, struktura automatycznie obsługuje menu dynamiczne, które można rozszerzyć podczas dostosowywania. Struktura kojarzy każdy nowy element z poleceniem zewnętrznym. Struktura wywołuje te polecenia, gdy użytkownik wybiera odpowiedni element z menu **Narzędzia.**

Za każdym razem, gdy użytkownik dodaje nowy element, struktura tworzy nowy obiekt. Typ klasy dla nowego obiektu jest definiowany przez *pToolRTC*. Typ klasy *pToolRTC* musi pochodzić z [klasy CUserTool](../../mfc/reference/cusertool-class.md).

Aby uzyskać więcej informacji na temat narzędzi użytkownika i sposobu włączania ich do aplikacji, zobacz [Narzędzia zdefiniowane przez użytkownika](../../mfc/user-defined-tools.md).

## <a name="cwinappexexitinstance"></a><a name="exitinstance"></a>CWinAppEx::Zakończ nienasyk

```
virtual int ExitInstance();
```

### <a name="return-value"></a>Wartość zwracana

### <a name="remarks"></a>Uwagi

## <a name="cwinappexgetbinary"></a><a name="getbinary"></a>CWinAppEx::GetBinary

Odczytuje dane binarne z określonego klucza rejestru.

```
BOOL GetBinary(
    LPCTSTR lpszEntry,
    LPBYTE* ppData,
    UINT* pBytes);
```

### <a name="parameters"></a>Parametry

*lpszEntry*<br/>
[w] Ciąg zawierający nazwę klucza rejestru.

*ppData (dane)*<br/>
[na zewnątrz] Wskaźnik do buforu, który metoda wypełnia danymi binarnymi.

*p Bajty*<br/>
[na zewnątrz] Wskaźnik do niepodpisanej liczby całkowitej, której metoda używa do zapisu liczby odczytanych bajtów.

### <a name="return-value"></a>Wartość zwracana

PRAWDA, jeśli się powiedzie; FAŁSZ inaczej.

### <a name="remarks"></a>Uwagi

Ta metoda odczytuje dane binarne zapisane w rejestrze. Aby zapisać dane w rejestrze, należy użyć metod [CWinAppEx::WriteBinary](#writebinary) i [CWinAppEx::WriteSectionBinary](#writesectionbinary).

Parametr *lpszEntry* to nazwa wpisu rejestru znajdującego się pod domyślnym kluczem rejestru dla aplikacji. Aby uzyskać lub ustawić domyślny klucz rejestru, należy użyć metod [CWinAppEx::GetRegistryBase](#getregistrybase) i [CWinAppEx::SetRegistryBase](#setregistrybase) odpowiednio.

## <a name="cwinappexgetcontextmenumanager"></a><a name="getcontextmenumanager"></a>CWinAppEx::GetContextMenuManager

Zwraca wskaźnik do globalnego [obiektu CContextMenuManager.](../../mfc/reference/ccontextmenumanager-class.md)

```
CContextMenuManager* GetContextMenuManager();
```

### <a name="return-value"></a>Wartość zwracana

Wskaźnik do obiektu `CContextMenuManager` globalnego.

### <a name="remarks"></a>Uwagi

Jeśli obiekt CContextMenuManager nie jest inicjowany, ta funkcja wywołuje [CWinAppEx::InitContextMenuManager,](#initcontextmenumanager) zanim zwróci wskaźnik.

## <a name="cwinappexgetdataversion"></a><a name="getdataversion"></a>CWinAppEx::GetDataVersion

```
int GetDataVersion() const;
```

### <a name="return-value"></a>Wartość zwracana

### <a name="remarks"></a>Uwagi

## <a name="cwinappexgetdataversionmajor"></a><a name="getdataversionmajor"></a>CWinAppEx::GetDataVersionMajor

Zwraca główną wersję aplikacji, która jest zapisywana w rejestrze systemu Windows podczas wywoływania [CWinAppEx::SaveState](#savestate).

```
int GetDataVersionMajor() const;
```

### <a name="return-value"></a>Wartość zwracana

Wartość całkowita zawierająca numer głównej wersji.

## <a name="cwinappexgetdataversionminor"></a><a name="getdataversionminor"></a>CWinAppEx::GetDataVersionMinor

Zwraca pomocniczą wersję aplikacji zapisaną w rejestrze systemu Windows podczas wywoływania [CWinAppEx::SaveState](#savestate).

```
int GetDataVersionMinor() const;
```

### <a name="return-value"></a>Wartość zwracana

Wartość całkowita zawierająca numer wersji pomocniczej.

## <a name="cwinappexgetint"></a><a name="getint"></a>CWinAppEx::GetInt

Odczytuje dane całkowite z określonego klucza rejestru.

```
int GetInt(
    LPCTSTR lpszEntry,
    int nDefault = 0);
```

### <a name="parameters"></a>Parametry

*lpszEntry*<br/>
[w] Ciąg zawierający nazwę wpisu rejestru.

*nDefault (Domyślne)*<br/>
[w] Wartość domyślna zwracana przez metodę, jeśli określony wpis rejestru nie istnieje.

### <a name="return-value"></a>Wartość zwracana

Dane rejestru, jeśli metoda zakończyła się pomyślnie; w przeciwnym razie *nDefault*.

### <a name="remarks"></a>Uwagi

Ta metoda odczytuje dane całkowite z rejestru. Jeśli z kluczem rejestru wskazanym przez *lpszEntry*nie ma żadnych danych całkowitych, ta metoda zwraca *wartość nDefault*. Aby zapisać dane w rejestrze, należy użyć metod [CWinAppEx::WriteSectionInt](#writesectionint) i [CWinAppEx::WriteInt](#writeint).

Parametr *lpszEntry* to nazwa wpisu rejestru znajdującego się pod domyślnym kluczem rejestru dla aplikacji. Aby uzyskać lub ustawić domyślny klucz rejestru, należy użyć metod [CWinAppEx::GetRegistryBase](#getregistrybase) i [CWinAppEx::SetRegistryBase](#setregistrybase) odpowiednio.

## <a name="cwinappexgetkeyboardmanager"></a><a name="getkeyboardmanager"></a>CWinAppEx::GetKeyboardManager

Zwraca wskaźnik do globalnego [obiektu CKeyboardManager.](../../mfc/reference/ckeyboardmanager-class.md)

```
CKeyboardManager* GetKeyboardManager();
```

### <a name="return-value"></a>Wartość zwracana

Wskaźnik do obiektu `CKeyboardManager` globalnego.

### <a name="remarks"></a>Uwagi

Jeśli menedżer klawiatury nie jest inicjowany, ta funkcja wywołuje [CWinAppEx::InitKeyboardManager,](#initkeyboardmanager) zanim zwróci wskaźnik.

## <a name="cwinappexgetmousemanager"></a><a name="getmousemanager"></a>CWinAppEx::GetMouseManager

Zwraca wskaźnik do globalnego [obiektu CMouseManager.](../../mfc/reference/cmousemanager-class.md)

```
CMouseManager* GetMouseManager();
```

### <a name="return-value"></a>Wartość zwracana

Wskaźnik do obiektu `CMouseManager` globalnego.

### <a name="remarks"></a>Uwagi

Jeśli menedżer myszy nie jest inicjowany, ta funkcja wywołuje [CWinAppEx::InitMouseManager,](#initmousemanager) zanim zwróci wskaźnik.

## <a name="cwinappexgetobject"></a><a name="getobject"></a>CWinAppEx::GetObject

Odczytuje [CObject](../../mfc/reference/cobject-class.md)-dervied danych z rejestru.

```
BOOL GetObject(
    LPCTSTR lpszEntry,
    CObject& obj);
```

### <a name="parameters"></a>Parametry

*lpszEntry*<br/>
[w] Ciąg zawierający ścieżkę względną wpisu rejestru.

*Obj*<br/>
[na zewnątrz] Odwołanie do `CObject`. Metoda używa tego odwołania do przechowywania danych rejestru.

### <a name="return-value"></a>Wartość zwracana

Nonzero, jeśli metoda zakończyła się pomyślnie; w przeciwnym razie 0.

### <a name="remarks"></a>Uwagi

Ta metoda odczytuje dane z rejestru, który pochodzi od `CObject`. Aby `CObject` zapisać dane w rejestrze, użyj [CWinAppEx::WriteObject](#writeobject) lub [CWinAppEx::WriteSectionObject](#writesectionobject).

Parametr *lpszEntry* to nazwa wpisu rejestru znajdującego się pod domyślnym kluczem rejestru dla aplikacji. Aby uzyskać lub ustawić domyślny klucz rejestru, należy użyć metod [CWinAppEx::GetRegistryBase](#getregistrybase) i [CWinAppEx::SetRegistryBase](#setregistrybase) odpowiednio.

## <a name="cwinappexgetregistrybase"></a><a name="getregistrybase"></a>CWinAppEx::GetRegistryBase

Pobiera domyślną ścieżkę rejestru dla aplikacji.

```
LPCTSTR GetRegistryBase();
```

### <a name="return-value"></a>Wartość zwracana

Ciąg zawierający ścieżkę domyślnej lokalizacji rejestru.

### <a name="remarks"></a>Uwagi

Wszystkie metody [klasy CWinAppEx,](../../mfc/reference/cwinappex-class.md) które uzyskują dostęp do rejestru, rozpoczynają się w lokalizacji domyślnej. Ta metoda służy do pobierania ścieżki domyślnej lokalizacji rejestru. Użyj [CWinAppEx::SetRegistryBase,](#setregistrybase) aby zmienić domyślną lokalizację rejestru.

## <a name="cwinappexgetregsectionpath"></a><a name="getregsectionpath"></a>CWinAppEx::GetRegSectionPath

Tworzy i zwraca ścieżkę bezwzględną klucza rejestru.

```
CString GetRegSectionPath(LPCTSTR szSectionAdd = _T(""));
```

### <a name="parameters"></a>Parametry

*szSectionAdd*<br/>
[w] Ciąg zawierający ścieżkę względną klucza rejestru.

### <a name="return-value"></a>Wartość zwracana

A, `CString` który zawiera ścieżkę bezwzględną klucza rejestru.

### <a name="remarks"></a>Uwagi

Ta metoda definiuje ścieżkę bezwzględną klucza rejestru, dołączając ścieżkę względną w *szSectionAdd* do domyślnej lokalizacji rejestru dla aplikacji. Aby uzyskać domyślny klucz rejestru, użyj metody [CWinAppEx::GetRegistryBase](#getregistrybase).

## <a name="cwinappexgetsectionbinary"></a><a name="getsectionbinary"></a>CWinAppEx::GetSectionBinary

Odczytuje dane binarne z rejestru.

```
BOOL GetSectionBinary(
    LPCTSTR lpszSubSection,
    LPCTSTR lpszEntry,
    LPBYTE* ppData,
    UINT* pBytes);
```

### <a name="parameters"></a>Parametry

*lpszSubSection*<br/>
[w] Ciąg zawierający ścieżkę względną klucza rejestru.

*lpszEntry*<br/>
[w] Ciąg, który zawiera wartość do odczytu.

*ppData (dane)*<br/>
[na zewnątrz] Wskaźnik do buforu, w którym metoda przechowuje dane.

*p Bajty*<br/>
[na zewnątrz] Wskaźnik do niepodpisanej liczby całkowitej. Metoda zapisuje rozmiar *ppData* do tego parametru.

### <a name="return-value"></a>Wartość zwracana

PRAWDA, jeśli się powiedzie; w przeciwnym razie FALSE.

### <a name="remarks"></a>Uwagi

Ta metoda odczytuje dane binarne zapisywane w rejestrze przy użyciu metod [CWinAppEx::WriteBinary](#writebinary) i [CWinAppEx::WriteSectionBinary](#writesectionbinary).

Parametr *lpszSubSection* nie jest ścieżką bezwzględną dla wpisu rejestru. Jest to ścieżka względna, która jest dołączana na końcu domyślnego klucza rejestru dla aplikacji. Aby uzyskać lub ustawić domyślny klucz rejestru, należy użyć metod [CWinAppEx::GetRegistryBase](#getregistrybase) i [CWinAppEx::SetRegistryBase](#setregistrybase) odpowiednio.

## <a name="cwinappexgetsectionint"></a><a name="getsectionint"></a>CWinAppEx::GetSectionInt

Odczytuje dane całkowite z rejestru.

```
int GetSectionInt(
    LPCTSTR lpszSubSection,
    LPCTSTR lpszEntry,
    int nDefault = 0);
```

### <a name="parameters"></a>Parametry

*lpszSubSection*<br/>
[w] Ciąg zawierający ścieżkę względną klucza rejestru.

*lpszEntry*<br/>
[w] Ciąg, który zawiera wartość do odczytu.

*nDefault (Domyślne)*<br/>
[w] Wartość domyślna do zwrócenia, jeśli określona wartość nie istnieje.

### <a name="return-value"></a>Wartość zwracana

Dane całkowite przechowywane w określonej wartości rejestru; *nDefault* jeśli dane nie istnieją.

### <a name="remarks"></a>Uwagi

Użyj metod [CWinAppEx::WriteInt](#writeint) i [CWinAppEx::WriteSectionInt](#writesectionint) do zapisu danych całkowitych do rejestru.

Parametr *lpszSubSection* nie jest bezwzględną ścieżką wpisu rejestru. Jest to ścieżka względna, która jest dodawana na końcu domyślnego klucza rejestru dla aplikacji. Aby uzyskać lub ustawić domyślny klucz rejestru, należy użyć metod [CWinAppEx::GetRegistryBase](#getregistrybase) i [CWinAppEx::SetRegistryBase](#setregistrybase) odpowiednio.

## <a name="cwinappexgetsectionobject"></a><a name="getsectionobject"></a>CWinAppEx::GetSectionObject

Odczytuje dane rejestru [CObject](../../mfc/reference/cobject-class.md) z rejestru.

```
BOOL GetSectionObject(
    LPCTSTR lpszSubSection,
    LPCTSTR lpszEntry,
    CObject& obj);
```

### <a name="parameters"></a>Parametry

*lpszSubSection*<br/>
[w] Ciąg zawierający ścieżkę względną klucza rejestru.

*lpszEntry*<br/>
[w] Ciąg, który zawiera wartość do odczytu.

*Obj*<br/>
[na zewnątrz] Odwołanie do `CObject`. Metoda używa tego `CObject` do przechowywania danych rejestru.

### <a name="return-value"></a>Wartość zwracana

Nonzero jeśli się powiedzie; w przeciwnym razie 0.

### <a name="remarks"></a>Uwagi

Ta metoda odczytuje dane z rejestru. Odczytywane `CObject` dane to dane lub dane `CObject`dla klasy wywodzącej się z . Aby `CObject` zapisać dane w rejestrze, użyj [CWinAppEx::WriteObject](#writeobject) lub [CWinAppEx::WriteSectionObject](#writesectionobject).

Parametr *lpszSubSection* nie jest ścieżką bezwzględną dla wpisu rejestru. Jest to ścieżka względna, która jest dołączana na końcu domyślnego klucza rejestru dla aplikacji. Aby uzyskać lub ustawić domyślny klucz rejestru, należy użyć metod [CWinAppEx::GetRegistryBase](#getregistrybase) i [CWinAppEx::SetRegistryBase](#setregistrybase) odpowiednio.

## <a name="cwinappexgetsectionstring"></a><a name="getsectionstring"></a>CWinAppEx::GetSectionString

Odczytuje dane ciągu z rejestru.

```
CString GetSectionString(
    LPCTSTR lpszSubSection,
    LPCTSTR lpszEntry,
    LPCTSTR lpszDefault = _T(""));
```

### <a name="parameters"></a>Parametry

*lpszSubSection*<br/>
[w] Ciąg zawierający ścieżkę względną klucza rejestru.

*lpszEntry*<br/>
[w] Ciąg, który zawiera wartość do odczytu.

*lpszDefault (ł.*<br/>
[w] Wartość domyślna do zwrócenia, jeśli określona wartość nie istnieje.

### <a name="return-value"></a>Wartość zwracana

Dane ciągu przechowywane w określonej wartości rejestru, jeśli dane istnieją; w przeciwnym razie *lpszDefault*.

### <a name="remarks"></a>Uwagi

Ta metoda odczytuje dane ciągów zapisane w rejestrze. Użyj [CWinAppEx::WriteString](#writestring) i [CWinAppEx::WriteSectionString](#writesectionstring) do zapisywania danych ciągów w rejestrze.

Parametr *lpszSubSection* nie jest ścieżką bezwzględną dla wpisu rejestru. Jest to ścieżka względna, która jest dołączana na końcu domyślnego klucza rejestru dla aplikacji. Aby uzyskać lub ustawić domyślny klucz rejestru, należy użyć metod [CWinAppEx::GetRegistryBase](#getregistrybase) i [CWinAppEx::SetRegistryBase](#setregistrybase) odpowiednio.

## <a name="cwinappexgetshellmanager"></a><a name="getshellmanager"></a>CWinAppEx::Menedżer GetShell

Zwraca wskaźnik do globalnego [obiektu CShellManager.](../../mfc/reference/cshellmanager-class.md)

```
CShellManager* GetShellManager();
```

### <a name="return-value"></a>Wartość zwracana

Wskaźnik do obiektu `CShellManager` globalnego.

### <a name="remarks"></a>Uwagi

Jeśli `CShellManager` obiekt nie jest inicjowany, ta funkcja wywołuje [CWinAppEx::InitShellManager,](#initshellmanager) zanim zwróci wskaźnik.

## <a name="cwinappexgetstring"></a><a name="getstring"></a>CWinAppEx::GetString

Odczytuje dane ciągu z określonego klucza rejestru.

```
CString GetString(
    LPCTSTR lpszEntry,
    LPCTSTR lpzDefault= _T(""));
```

### <a name="parameters"></a>Parametry

*lpszEntry*<br/>
[w] Ciąg zawierający nazwę klucza rejestru

*lpzDefault (lpzDefault)*<br/>
[w] Wartość domyślna zwracana przez metodę, jeśli określony wpis rejestru nie istnieje.

### <a name="return-value"></a>Wartość zwracana

Dane ciągu przechowywane w rejestrze, jeśli zakończy się pomyślnie; *lpszDefault* inaczej.

### <a name="remarks"></a>Uwagi

Ta metoda odczytuje dane ciągów zapisane w rejestrze. Aby zapisać dane w rejestrze, użyj metod [CWinAppEx::WriteString](#writestring) lub [CWinAppEx::WriteSectionString](#writesectionstring).

Parametr *lpszEntry* to nazwa wpisu rejestru znajdującego się pod domyślnym kluczem rejestru dla aplikacji. Aby uzyskać lub ustawić domyślny klucz rejestru, należy użyć metod [CWinAppEx::GetRegistryBase](#getregistrybase) i [CWinAppEx::SetRegistryBase](#setregistrybase) odpowiednio.

## <a name="cwinappexgettooltipmanager"></a><a name="gettooltipmanager"></a>CWinAppEx::GetTooltipManager

Zwraca wskaźnik do globalnego [obiektu CTooltipManager.](../../mfc/reference/ctooltipmanager-class.md)

```
CTooltipManager* GetTooltipManager();
```

### <a name="return-value"></a>Wartość zwracana

Wskaźnik do obiektu `CTooltipManager` globalnego.

### <a name="remarks"></a>Uwagi

Jeśli `CTooltipManager` obiekt nie jest inicjowany, ta funkcja wywołuje [CWinAppEx::InitTooltipManager](#inittooltipmanager) przed zwraca wskaźnik.

## <a name="cwinappexgetusertoolsmanager"></a><a name="getusertoolsmanager"></a>CWinAppEx::GetUserToolsManager

Zwraca wskaźnik do globalnego [obiektu CUserToolsManager.](../../mfc/reference/cusertoolsmanager-class.md)

```
CUserToolsManager* GetUserToolsManager();
```

### <a name="return-value"></a>Wartość zwracana

Wskaźnik do obiektu `CUserToolsManager` globalnego; NULL, jeśli zarządzanie narzędziami użytkownika nie jest włączone dla aplikacji.

### <a name="remarks"></a>Uwagi

Przed pobraniem wskaźnika `CUserToolsManager` do obiektu należy zainicjować menedżera, wywołując [CWinAppEx::EnableUserTools](#enableusertools).

## <a name="cwinappexinitcontextmenumanager"></a><a name="initcontextmenumanager"></a>CWinAppEx::InitContextMenuManager

Inicjuje [CContextMenuManager](../../mfc/reference/ccontextmenumanager-class.md) obiektu.

```
BOOL InitContextMenuManager();
```

### <a name="return-value"></a>Wartość zwracana

Nonzero, jeśli metoda tworzy CContextMenuManager obiektu; 0, `CContextMenuManager` jeśli obiekt już istnieje.

### <a name="remarks"></a>Uwagi

Jeśli wywołasz [CWinAppEx::GetContextMenuManager](#getcontextmenumanager), domyślna `InitContextMenuManager`implementacja tej metody wywołuje .

Jeśli aplikacja ma już menedżera menu `InitContextMenuManager`kontekstowego i wywołać, aplikacja będzie mieć [błąd ASSERT.](diagnostic-services.md#assert) W związku z tym `InitContextMenuManager` nie należy `CContextMenuManager` wywoływać, jeśli obiekt jest utworzony bezpośrednio. Jeśli obiekt nie jest `CContextMenuManager`używany, `GetContextMenuManager` należy użyć `CContextMenuManager` do utworzenia obiektu.

## <a name="cwinappexinitkeyboardmanager"></a><a name="initkeyboardmanager"></a>CWinAppEx::InitKeyboardManager

Inicjuje [CKeyboardManager](../../mfc/reference/ckeyboardmanager-class.md) obiektu.

```
BOOL InitKeyboardManager();
```

### <a name="return-value"></a>Wartość zwracana

Nonzero, jeśli metoda `CKeyboardManager` tworzy obiekt; 0, `CKeyboardManager` jeśli obiekt już istnieje.

### <a name="remarks"></a>Uwagi

Jeśli wywołasz [CWinAppEx::GetKeyboardManager](#getkeyboardmanager), domyślna `InitKeyboardManager`implementacja tej metody wywołuje .

Jeśli aplikacja ma już menedżera klawiatury i wywołać, `InitKeyboardManager`aplikacja będzie mieć błąd [ASSERT.](diagnostic-services.md#assert) W związku z tym `InitKeyboardManager` nie należy `CKeyboardManager` wywoływać, jeśli obiekt jest utworzony bezpośrednio. Jeśli obiekt nie jest `CKeyboardManager`używany, `GetKeyboardManager` należy użyć `CKeyboardManager` do utworzenia obiektu.

## <a name="cwinappexinitmousemanager"></a><a name="initmousemanager"></a>CWinAppEx::InitMouseManager

Inicjuje [obiekt CMouseManager.](../../mfc/reference/cmousemanager-class.md)

```
BOOL InitMouseManager();
```

### <a name="return-value"></a>Wartość zwracana

Nonzero, jeśli metoda `CMouseManager` tworzy obiekt; 0, `CMouseManager` jeśli obiekt już istnieje.

### <a name="remarks"></a>Uwagi

Jeśli wywołasz [CWinAppEx::GetMouseManager](#getmousemanager), domyślna `InitMouseManager`implementacja tej metody wywołuje .

Jeśli aplikacja ma już menedżera myszy `InitMouseManager`i wywołać, aplikacja będzie mieć błąd [ASSERT.](diagnostic-services.md#assert) W związku z `InitMouseManager` tym nie `CMouseManager` należy wywoływać, jeśli obiekt jest utworzony bezpośrednio. Jeśli obiekt nie jest `CMouseManager`używany, `GetMouseManager` należy użyć `CMouseManager` do utworzenia obiektu.

## <a name="cwinappexinitshellmanager"></a><a name="initshellmanager"></a>CWinAppEx::InitShellManager

Inicjuje [CShellManager](../../mfc/reference/cshellmanager-class.md) obiektu.

```
BOOL InitShellManager();
```

### <a name="return-value"></a>Wartość zwracana

Nonzero, jeśli metoda `CShellManager` tworzy obiekt; 0, `CShellManager` jeśli obiekt już istnieje.

### <a name="remarks"></a>Uwagi

Jeśli wywołasz [CWinAppEx::GetShellManager](#getshellmanager), domyślna `InitShellManager`implementacja tej metody wywołuje .

Jeśli aplikacja ma już menedżera powłoki i wywołać, `InitShellManager`aplikacja podnosi błąd [ASSERT.](diagnostic-services.md#assert) W związku z `InitShellManager` tym nie `CShellManager` należy wywoływać, jeśli obiekt zostanie utworzony bezpośrednio. Jeśli obiekt nie jest `CShellManager`używany `GetShellManager` do `CShellManager` niestandardowego, należy użyć do utworzenia obiektu.

## <a name="cwinappexinittooltipmanager"></a><a name="inittooltipmanager"></a>CWinAppEx::InitTooltipManager

Inicjuje [CTooltipManager](../../mfc/reference/ctooltipmanager-class.md) obiektu.

```
BOOL InitTooltipManager();
```

### <a name="return-value"></a>Wartość zwracana

Nonzero, jeśli metoda `CTooltipManager` tworzy obiekt; 0, `CTooltipManager` jeśli obiekt już istnieje.

### <a name="remarks"></a>Uwagi

Jeśli wywołasz [CWinAppEx::GetTooltipManager](#gettooltipmanager), domyślna `InitTooltipManager`implementacja tej metody wywołuje .

Jeśli aplikacja ma już menedżera etykietki `InitTooltipManager`narzędzia i wywołać, aplikacja będzie mieć błąd [ASSERT.](diagnostic-services.md#assert) W związku z tym `InitTooltipManager` nie należy `CTooltipManager` wywoływać, jeśli obiekt jest utworzony bezpośrednio. Jeśli obiekt nie jest `CTooltipManager`używany, `GetTooltipManager` należy użyć `CTooltipManager` do utworzenia obiektu.

## <a name="cwinappexisresourcesmartupdate"></a><a name="isresourcesmartupdate"></a>CWinAppEx::IsResourceSmartUpdate

```
BOOL IsResourceSmartUpdate() const;
```

### <a name="return-value"></a>Wartość zwracana

### <a name="remarks"></a>Uwagi

## <a name="cwinappexisstateexists"></a><a name="isstateexists"></a>CWinAppEx::IsStateExists

Wskazuje, czy określony klucz znajduje się w rejestrze.

```
BOOL IsStateExists(LPCTSTR lpszSectionName);
```

### <a name="parameters"></a>Parametry

*lpszSectionName*<br/>
[w] Ciąg zawierający ścieżkę klucza rejestru.

### <a name="return-value"></a>Wartość zwracana

Nonzero, jeśli klucz znajduje się w rejestrze; w przeciwnym razie 0.

## <a name="cwinappexloadcustomstate"></a><a name="loadcustomstate"></a>CWinAppEx::LoadCustomState

Struktura wywołuje tę metodę po załadowaniu stanu aplikacji z rejestru.

```
virtual void LoadCustomState();
```

### <a name="remarks"></a>Uwagi

Zastąd w tej metodzie należy wykonać dowolną metodę przetwarzania po załadowaniu przez aplikację stanu z rejestru. Domyślnie ta metoda nic nie robi.

Aby załadować niestandardowe informacje o stanie z rejestru, informacje muszą być najpierw zapisane przy użyciu [CWinAppEx::SaveCustomState](#savecustomstate).

## <a name="cwinappexloadstate"></a><a name="loadstate"></a>CWinAppEx::Stan obciążenia

Odczytuje stan aplikacji z rejestru systemu Windows.

```
BOOL LoadState(
    CMDIFrameWndEx* pFrame,
    LPCTSTR lpszSectionName = NULL);

BOOL LoadState(
    CFrameWndEx* pFrame,
    LPCTSTR lpszSectionName = NULL);

BOOL LoadState(
    COleIPFrameWndEx* pFrame,
    LPCTSTR lpszSectionName = NULL);

virtual BOOL LoadState(
    LPCTSTR lpszSectionName = NULL,
    CFrameImpl* pFrameImpl = NULL);
```

### <a name="parameters"></a>Parametry

*pFrame (klatka)*<br/>
[w] Wskaźnik do obiektu okna ramki. Metoda stosuje informacje o stanie w rejestrze do tego okna ramki.

*lpszSectionName*<br/>
[w] Ciąg zawierający ścieżkę względną klucza rejestru.

*pFrameImpl (wimpl)*<br/>
[w] Wskaźnik do `CFrameImpl` obiektu. Metoda stosuje informacje o stanie w rejestrze do tego okna ramki.

### <a name="return-value"></a>Wartość zwracana

Nonzero jeśli się powiedzie; 0 w przeciwnym razie.

### <a name="remarks"></a>Uwagi

Ta metoda ładuje stan aplikacji i wszelkie informacje o stanie dla okna ramki. Załadowane informacje dla okna ramki są stosowane do dołączonego okna ramki. Jeśli okno ramki nie zostanie podane, zostaną załadowane tylko informacje o stanie aplikacji. Informacje o aplikacji obejmują stan [CMouseManager Class](../../mfc/reference/cmousemanager-class.md), [CContextMenuManager Class](../../mfc/reference/ccontextmenumanager-class.md), [CKeyboardManager Class](../../mfc/reference/ckeyboardmanager-class.md)i [CUserToolsManager Class](../../mfc/reference/cusertoolsmanager-class.md).

Domyślna implementacja `LoadState`wywołań `CFrameImpl::OnLoadFrame` .

Parametr *lpszSectionName* nie jest ścieżką bezwzględną wpisu rejestru. Jest to ścieżka względna, która jest dodawana na końcu domyślnego klucza rejestru dla aplikacji. Aby uzyskać lub ustawić domyślny klucz rejestru, należy użyć metod [CWinAppEx::GetRegistryBase](#getregistrybase) i [CWinAppEx::SetRegistryBase](#setregistrybase) odpowiednio.

## <a name="cwinappexloadwindowplacement"></a><a name="loadwindowplacement"></a>CWinAppEx::LoadWindowPlacement

Wywoływana przez platformę, gdy ładuje rozmiar i lokalizację okna ramki głównej z rejestru.

```
virtual BOOL LoadWindowPlacement(
    CRect& rectNormalPosition,
    int& nFlags,
    int& nShowCmd);
```

### <a name="parameters"></a>Parametry

*reectNormalPosition (pozycja 3000)*<br/>
[na zewnątrz] Prostokąt zawierający współrzędne okna ramki głównej, gdy znajduje się w pozycji przywróconej.

*nPłgi*<br/>
[na zewnątrz] Flagi, które kontrolują położenie zminimalizowanego okna i sposób przełączania się systemu operacyjnego między zminimalizowanym oknem a przywróconym oknem.

*nShowCmd*<br/>
[na zewnątrz] Liczba całkowita określająca stan pokazu okna. Aby uzyskać więcej informacji na temat możliwych wartości, zobacz [CWnd::ShowWindow](../../mfc/reference/cwnd-class.md#showwindow).

### <a name="return-value"></a>Wartość zwracana

Nonzero jeśli się powiedzie; 0 w przeciwnym razie.

### <a name="remarks"></a>Uwagi

Domyślnie MFC automatycznie ładuje poprzedniej pozycji i stanu okna ramki głównej po uruchomieniu aplikacji. Aby uzyskać więcej informacji o tym, jak te informacje są przechowywane w rejestrze, zobacz [CWinAppEx::StoreWindowPlacement](#storewindowplacement).

Zastąd w tej metodzie należy załadować dodatkowe informacje o oknie ramki głównej.

## <a name="cwinappexm_bforceimagereset"></a><a name="m_bforceimagereset"></a>CWinAppEx::m_bForceImageReset

Określa, czy struktura resetuje wszystkie obrazy paska narzędzi po ponownym załadowaniu okna ramki zawierającego pasek narzędzi.

```
BOOL m_bForceImageReset;
```

### <a name="remarks"></a>Uwagi

Element `m_bForceImageReset` członkowski danych jest zmienną chroniona.

## <a name="cwinappexonappcontexthelp"></a><a name="onappcontexthelp"></a>CWinAppEx::OnAppContextHelp

Struktura wywołuje tę metodę, gdy użytkownik żąda pomocy kontekstowej dla okna dialogowego **Dostosowywanie.**

```
virtual void OnAppContextHelp(
    CWnd* pWndControl,
    const DWORD dwHelpIDArray[]);
```

### <a name="parameters"></a>Parametry

*pWdControl (Kontrola pWndControl)*<br/>
[w] Wskaźnik do obiektu okna, dla którego użytkownik wywołał pomoc kontekstu.

*dwHelpIDArray*<br/>
[w] Wartość zarezerwowana.

### <a name="remarks"></a>Uwagi

Ta metoda jest obecnie zarezerwowana do użytku w przyszłości. Domyślna implementacja nic nie robi i obecnie nie jest wywoływana przez platformę.

## <a name="cwinappexonclosingmainframe"></a><a name="onclosingmainframe"></a>CWinAppEx::OnClosingMainFrame

Struktura wywołuje tę metodę, gdy okno ramki przetwarza WM_CLOSE.

```
virtual void OnClosingMainFrame(CFrameImpl* pFrameImpl);
```

### <a name="parameters"></a>Parametry

*pFrameImpl (wimpl)*<br/>
[w] Wskaźnik do `CFrameImpl` obiektu.

### <a name="remarks"></a>Uwagi

Domyślna implementacja tej metody zapisuje stan *pFrameImpl*.

## <a name="cwinappexonviewdoubleclick"></a><a name="onviewdoubleclick"></a>CWinAppEx::OnViewDoubleClick

Wywołuje polecenie zdefiniowane przez użytkownika, które jest skojarzone z widokiem, gdy użytkownik kliknie dwukrotnie w dowolnym miejscu w tym widoku.

```
virtual BOOL OnViewDoubleClick(
    CWnd* pWnd,
    int iViewId);
```

### <a name="parameters"></a>Parametry

*Pwnd*<br/>
[w] Wskaźnik do obiektu pochodzącego z [klasy CView](../../mfc/reference/cview-class.md).

*Identyfikator iViewId*<br/>
[w] Identyfikator widoku.

### <a name="return-value"></a>Wartość zwracana

PRAWDA, jeśli struktura znajdzie polecenie; w przeciwnym razie FALSE.

### <a name="remarks"></a>Uwagi

Aby obsługiwać niestandardowe zachowanie myszy, należy wywołać tę funkcję podczas przetwarzania wiadomości WM_LBUTTONDBLCLK. Ta metoda wykona polecenie skojarzone z identyfikatorem widoku dostarczonym przez *iViewId*. Aby uzyskać więcej informacji na temat niestandardowego zachowania myszy, zobacz [Dostosowywanie klawiatury i myszy](../../mfc/keyboard-and-mouse-customization.md).

## <a name="cwinappexonworkspaceidle"></a><a name="onworkspaceidle"></a>CWinAppEx::OnWorkspaceIdle

```
virtual BOOL OnWorkspaceIdle(CWnd*);
```

### <a name="parameters"></a>Parametry

[w] *&#38;CWnd*<br/>

### <a name="return-value"></a>Wartość zwracana

### <a name="remarks"></a>Uwagi

## <a name="cwinappexpreloadstate"></a><a name="preloadstate"></a>CWinAppEx::PreLoadState

Struktura wywołuje tę metodę bezpośrednio przed załadowaniem stanu aplikacji z rejestru.

```
virtual void PreLoadState();
```

### <a name="remarks"></a>Uwagi

Zastądeń tę metodę, jeśli chcesz wykonać wszelkie przetwarzanie bezpośrednio przed ramą ładuje stan aplikacji.

## <a name="cwinappexpresavestate"></a><a name="presavestate"></a>CWinAppEx::PreSaveState

Struktura wywołuje tę metodę bezpośrednio przed zapisaniem stanu aplikacji.

```
virtual void PreSaveState();
```

### <a name="remarks"></a>Uwagi

Zastądź tę metodę, jeśli chcesz wykonać wszelkie przetwarzanie bezpośrednio przed platformą zapisuje stan aplikacji.

## <a name="cwinappexreloadwindowplacement"></a><a name="reloadwindowplacement"></a>CWinAppEx::ReloadWindowPlacement

Ponownie ładuje rozmiar i lokalizację okna z rejestru.

```
virtual BOOL ReloadWindowPlacement(CFrameWnd* pFrame);
```

### <a name="parameters"></a>Parametry

*pFrame (klatka)*<br/>
[w] Wskaźnik do okna ramki.

### <a name="return-value"></a>Wartość zwracana

Nonzero, jeśli metoda zakończyła się pomyślnie; 0, jeśli obciążenie nie powiodło się lub nie ma żadnych danych do załadowania.

### <a name="remarks"></a>Uwagi

Użyj funkcji [CWinAppEx::StoreWindowPlacement,](#storewindowplacement) aby zapisać rozmiar i lokalizację okna w rejestrze.

## <a name="cwinappexsavecustomstate"></a><a name="savecustomstate"></a>CWinAppEx::Zapisz stan

Struktura wywołuje tę metodę po zapisaniu stanu aplikacji w rejestrze.

```
virtual void SaveCustomState();
```

### <a name="remarks"></a>Uwagi

Zastąd w tej metodzie należy wykonać dowolną metodę przetwarzania po zapisaniu stanu w rejestrze. Domyślnie ta metoda nic nie robi.

## <a name="cwinappexsavestate"></a><a name="savestate"></a>CWinAppEx::Stan zapisu

Zapisuje stan aplikacji w rejestrze systemu Windows.

```
virtual BOOL SaveState(
    LPCTSTR lpszSectionName = NULL,
    CFrameImpl* pFrameImpl = NULL);

BOOL SaveState(
    CMDIFrameWndEx* pFrame,
    LPCTSTR lpszSectionName = NULL);

BOOL SaveState(
    CFrameWndEx* pFrame,
    LPCTSTR lpszSectionName = NULL);

BOOL SaveState(
    COleIPFrameWndEx* pFrame,
    LPCTSTR lpszSectionName = NULL);
```

### <a name="parameters"></a>Parametry

*lpszSectionName*<br/>
[w] Ciąg zawierający ścieżkę względną klucza rejestru.

*pFrameImpl (wimpl)*<br/>
[w] Wskaźnik do `CFrameImpl` obiektu. Ta ramka jest zapisywana w rejestrze systemu Windows.

*pFrame (klatka)*<br/>
[w] Wskaźnik do obiektu okna ramki. Ta ramka jest zapisywana w rejestrze systemu Windows.

### <a name="return-value"></a>Wartość zwracana

PRAWDA, jeśli się powiedzie; FAŁSZ inaczej.

### <a name="remarks"></a>Uwagi

Ta metoda zapisuje stan aplikacji i wszelkie informacje o stanie dla podanego okna ramki. Jeśli okno ramki nie zostanie podasz, metoda zapisuje tylko stan aplikacji. Informacje o aplikacji obejmują stan [CMouseManager Class](../../mfc/reference/cmousemanager-class.md), [CContextMenuManager Class](../../mfc/reference/ccontextmenumanager-class.md), [CKeyboardManager Class](../../mfc/reference/ckeyboardmanager-class.md)i [CUserToolsManager Class](../../mfc/reference/cusertoolsmanager-class.md).

Parametr *lpszSectionName* nie jest ścieżką bezwzględną wpisu rejestru. Jest to ścieżka względna, która jest dołączana na końcu domyślnego klucza rejestru dla aplikacji. Aby uzyskać lub ustawić domyślny klucz rejestru, należy użyć metod [CWinAppEx::GetRegistryBase](#getregistrybase) i [CWinAppEx::SetRegistryBase](#setregistrybase) odpowiednio.

## <a name="cwinappexsetregistrybase"></a><a name="setregistrybase"></a>CWinAppEx::SetRegistryBase

Ustawia domyślną ścieżkę rejestru dla aplikacji.

```
LPCTSTR SetRegistryBase(LPCTSTR lpszSectionName = NULL);
```

### <a name="parameters"></a>Parametry

*lpszSectionName*<br/>
[w] Ciąg zawierający ścieżkę klucza rejestru.

### <a name="return-value"></a>Wartość zwracana

Ciąg zawierający ścieżkę domyślnej lokalizacji rejestru.

### <a name="remarks"></a>Uwagi

Wszystkie metody [klasy CWinAppEx,](../../mfc/reference/cwinappex-class.md) które uzyskują dostęp do rejestru, rozpoczynają się w lokalizacji domyślnej. Ta metoda służy do zmiany tej domyślnej lokalizacji rejestru. Użyj [CWinAppEx::GetRegistryBase,](#getregistrybase) aby pobrać domyślną lokalizację rejestru.

## <a name="cwinappexshowpopupmenu"></a><a name="showpopupmenu"></a>CWinAppEx::ShowPopupMenu

Wyświetla menu podręczne.

```
virtual BOOL ShowPopupMenu(
    UINT uiMenuResId,
    const CPoint& point,
    CWnd* pWnd);
```

### <a name="parameters"></a>Parametry

*interfejs użytkownika uiMenuResId*<br/>
[w] Identyfikator zasobu menu.

*Punkt*<br/>
[w] Punkt [CPoint](../../atl-mfc-shared/reference/cpoint-class.md) określający położenie menu we współrzędnych ekranu.

*Pwnd*<br/>
[w] Wskaźnik do okna, które jest właścicielem menu podręcznego.

### <a name="return-value"></a>Wartość zwracana

Niezerowe, jeśli menu podręczne jest wyświetlane pomyślnie; 0 w przeciwnym razie.

### <a name="remarks"></a>Uwagi

Ta metoda wyświetla menu skojarzone z *uiMenuResId*.

Aby obsługiwać menu podręczne, musisz mieć obiekt [CContextMenuManager.](../../mfc/reference/ccontextmenumanager-class.md) Jeśli `CContextMenuManager` obiekt nie został zainicjowany, `ShowPopupMenu` zakończy się niepowodzeniem.

## <a name="cwinappexstorewindowplacement"></a><a name="storewindowplacement"></a>CWinAppEx::StoreWindowPlacement

Wywoływana przez strukturę, aby zapisać rozmiar i lokalizację okna ramki głównej do rejestru.

```
virtual BOOL StoreWindowPlacement(
    const CRect& rectNormalPosition,
    int nFlags,
    int nShowCmd);
```

### <a name="parameters"></a>Parametry

*nPłgi*<br/>
[w] Flagi, które kontrolują położenie zminimalizowanego okna i sposób przełączania się systemu operacyjnego między zminimalizowanym oknem a przywróconym oknem.

*nShowCmd*<br/>
[w] Liczba całkowita określająca stan pokazu okna. Aby uzyskać więcej informacji na temat możliwych wartości, zobacz [CWnd::ShowWindow](../../mfc/reference/cwnd-class.md#showwindow).

*reectNormalPosition (pozycja 3000)*<br/>
[w] Prostokąt zawierający współrzędne okna ramki głównej, gdy jest w stanie przywróconym.

### <a name="return-value"></a>Wartość zwracana

Nonzero jeśli się powiedzie; 0 w przeciwnym razie.

### <a name="remarks"></a>Uwagi

Domyślnie MFC automatycznie zapisuje położenie i stan okna ramki głównej przed zamknięciem aplikacji. Te informacje są przechowywane w rejestrze systemu Windows w obszarze klucza WindowPlacement w domyślnej lokalizacji rejestru dla aplikacji. Aby uzyskać więcej informacji na temat domyślnej lokalizacji rejestru aplikacji, zobacz [CWinAppEx::GetRegistryBase](#getregistrybase).

Zastąpokaj tę metodę, jeśli chcesz przechowywać dodatkowe informacje o oknie ramki głównej.

## <a name="cwinappexwritebinary"></a><a name="writebinary"></a>CWinAppEx::WriteBinary

Zapisuje dane binarne do rejestru.

```
BOOL WriteBinary(
    LPCTSTR lpszEntry,
    LPBYTE pData,
    UINT nBytes);
```

### <a name="parameters"></a>Parametry

*lpszEntry*<br/>
[w] Ciąg zawierający nazwę klucza rejestru.

*Pdata*<br/>
[w] Dane do przechowywania.

*n Bajty*<br/>
[w] Rozmiar *pData* w bajtach.

### <a name="return-value"></a>Wartość zwracana

PRAWDA, jeśli ta metoda zakończy się pomyślnie; w przeciwnym razie FALSE.

### <a name="remarks"></a>Uwagi

Parametr *lpszEntry* to nazwa wpisu rejestru znajdującego się pod domyślnym kluczem rejestru dla aplikacji. Aby uzyskać lub ustawić domyślny klucz rejestru, należy użyć metod [CWinAppEx::GetRegistryBase](#getregistrybase) i [CWinAppEx::SetRegistryBase](#setregistrybase) odpowiednio.

Jeśli klucz określony przez *lpszEntry* nie istnieje, ta metoda utworzy go.

## <a name="cwinappexwriteint"></a><a name="writeint"></a>CWinAppEx::WriteInt

Zapisuje dane liczbowe w rejestrze.

```
BOOL WriteInt(
    LPCTSTR lpszEntry,
    int nValue);
```

### <a name="parameters"></a>Parametry

*lpszEntry*<br/>
[w] Ciąg zawierający nazwę klucza rejestru.

*wartość nValue*<br/>
[w] Dane do przechowywania.

### <a name="return-value"></a>Wartość zwracana

PRAWDA, jeśli ta metoda zakończy się pomyślnie; w przeciwnym razie FALSE.

### <a name="remarks"></a>Uwagi

Parametr *lpszEntry* to nazwa wpisu rejestru znajdującego się pod domyślnym kluczem rejestru dla aplikacji. Aby uzyskać lub ustawić domyślny klucz rejestru, należy użyć metod [CWinAppEx::GetRegistryBase](#getregistrybase) i [CWinAppEx::SetRegistryBase](#setregistrybase) odpowiednio.

Jeśli klucz określony przez *lpszEntry* nie istnieje, ta metoda utworzy go.

## <a name="cwinappexwriteobject"></a><a name="writeobject"></a>CWinAppEx::WriteObject

Zapisuje dane pochodzące z [CObject Klasy](../../mfc/reference/cobject-class.md) do rejestru.

```
BOOL WriteObject(
    LPCTSTR lpszEntry,
    CObject& obj);
```

### <a name="parameters"></a>Parametry

*lpszEntry*<br/>
[w] Ciąg zawierający wartość do ustawionego.

*Obj*<br/>
[w] Odwołanie do `CObject` danych, które będzie przechowywać metoda.

### <a name="return-value"></a>Wartość zwracana

PRAWDA, jeśli ta metoda zakończy się pomyślnie; w przeciwnym razie FALSE.

### <a name="remarks"></a>Uwagi

Ta metoda zapisuje dane *obj* do określonej wartości w domyślnym kluczu rejestru. Użyj [CWinAppEx::GetRegistryBase,](#getregistrybase) aby określić bieżący klucz rejestru.

## <a name="cwinappexwritesectionbinary"></a><a name="writesectionbinary"></a>CWinAppEx::WriteSectionBinary

Zapisuje dane binarne do wartości w rejestrze.

```
BOOL WriteSectionBinary(
    LPCTSTR lpszSubSection,
    LPCTSTR lpszEntry,
    LPBYTE pData,
    UINT nBytes);
```

### <a name="parameters"></a>Parametry

*lpszSubSection*<br/>
[w] Ciąg zawierający nazwę klucza rejestru

*lpszEntry*<br/>
[w] Ciąg zawierający wartość do ustawionego.

*Pdata*<br/>
[w] Dane do zapisania w rejestrze.

*n Bajty*<br/>
[w] Rozmiar *pData* w bajtach.

### <a name="return-value"></a>Wartość zwracana

PRAWDA, jeśli ta metoda zakończy się pomyślnie; w przeciwnym razie FALSE.

### <a name="remarks"></a>Uwagi

Parametr *lpszSubSection* nie jest ścieżką bezwzględną wpisu rejestru. Jest to ścieżka względna, która jest dołączana na końcu domyślnego klucza rejestru dla aplikacji. Aby uzyskać lub ustawić domyślny klucz rejestru, należy użyć metod [CWinAppEx::GetRegistryBase](#getregistrybase) i [CWinAppEx::SetRegistryBase](#setregistrybase) odpowiednio.

Jeśli klucz określony przez *lpszEntry* nie istnieje, ta metoda utworzy go.

## <a name="cwinappexwritesectionint"></a><a name="writesectionint"></a>CWinAppEx::WriteSectionInt

Zapisuje dane liczbowe w rejestrze.

```
BOOL WriteSectionInt(
    LPCTSTR lpszSubSection,
    LPCTSTR lpszEntry,
    int nValue);
```

### <a name="parameters"></a>Parametry

*lpszSubSection*<br/>
[w] Ciąg zawierający ścieżkę względną klucza rejestru.

*lpszEntry*<br/>
[w] Ciąg zawierający wartość do ustawionego.

*wartość nValue*<br/>
[w] Dane do zapisania w rejestrze.

### <a name="return-value"></a>Wartość zwracana

PRAWDA, jeśli ta metoda zakończy się pomyślnie; w przeciwnym razie FALSE.

### <a name="remarks"></a>Uwagi

Parametr *lpszSubSection* nie jest ścieżką bezwzględną dla wpisu rejestru. Jest to ścieżka względna, która jest dołączana do domyślnego klucza rejestru dla aplikacji. Aby uzyskać lub ustawić domyślny klucz rejestru, należy użyć metod [CWinAppEx::GetRegistryBase](#getregistrybase) i [CWinAppEx::SetRegistryBase](#setregistrybase) odpowiednio.

Jeśli klucz określony przez *lpszEntry* nie istnieje, ta metoda utworzy go.

## <a name="cwinappexwritesectionobject"></a><a name="writesectionobject"></a>CWinAppEx::WriteSectionObject

Zapisuje dane pochodzące z [klasy CObject](../../mfc/reference/cobject-class.md) do określonej wartości rejestru.

```
BOOL WriteSectionObject(
    LPCTSTR lpszSubSection,
    LPCTSTR lpszEntry,
    CObject& obj);
```

### <a name="parameters"></a>Parametry

*lpszSubSection*<br/>
[w] Ciąg zawierający nazwę klucza rejestru.

*lpszEntry*<br/>
[w] Ciąg zawierający nazwę wartości do ustawionego.

*Obj*<br/>
[w] Dane do przechowywania.

### <a name="return-value"></a>Wartość zwracana

PRAWDA, jeśli ta metoda zakończy się pomyślnie; w przeciwnym razie FALSE.

### <a name="remarks"></a>Uwagi

Parametr *lpszSubSection* nie jest ścieżką bezwzględną dla wpisu rejestru. Jest to ścieżka względna, która jest dołączana na końcu domyślnego klucza rejestru dla aplikacji. Aby uzyskać lub ustawić domyślny klucz rejestru, należy użyć metod [CWinAppEx::GetRegistryBase](#getregistrybase) i [CWinAppEx::SetRegistryBase](#setregistrybase), odpowiednio.

Jeśli wartość określona przez *lpszEntry* nie istnieje w kluczu rejestru określonym przez *lpszSubSection*, ta metoda utworzy tę wartość.

## <a name="cwinappexwritesectionstring"></a><a name="writesectionstring"></a>CWinAppEx::WriteSectionString

Zapisuje dane ciągu do wartości w rejestrze.

```
BOOL WriteSectionString(
    LPCTSTR lpszSubSection,
    LPCTSTR lpszEntry,
    LPCTSTR lpszValue);
```

### <a name="parameters"></a>Parametry

*lpszSubSection*<br/>
[w] Ciąg zawierający nazwę klucza rejestru.

*lpszEntry*<br/>
[w] Ciąg zawierający wartość do ustawionego.

*lpszValue*<br/>
[w] Dane ciągu do zapisania w rejestrze.

### <a name="return-value"></a>Wartość zwracana

PRAWDA, jeśli ta metoda zakończy się pomyślnie; w przeciwnym razie FALSE.

### <a name="remarks"></a>Uwagi

Parametr *lpszSubSection* nie jest ścieżką bezwzględną dla wpisu rejestru. Jest to ścieżka względna, która jest dołączana na końcu domyślnego klucza rejestru dla aplikacji. Aby uzyskać lub ustawić domyślny klucz rejestru, należy użyć metod [CWinAppEx::GetRegistryBase](#getregistrybase) i [CWinAppEx::SetRegistryBase](#setregistrybase), odpowiednio.

Jeśli wartość określona przez *lpszEntry* nie istnieje w obszarze *lpszSubSection*, ta metoda ją utworzy.

## <a name="cwinappexwritestring"></a><a name="writestring"></a>CWinAppEx::WriteString

Zapisuje dane ciągu do rejestru.

```
BOOL WriteString(
    LPCTSTR lpszEntry,
    LPCTSTR lpszValue);
```

### <a name="parameters"></a>Parametry

*lpszEntry*<br/>
[w] Ciąg zawierający nazwę klucza rejestru.

*lpszValue*<br/>
[w] Dane do przechowywania.

### <a name="return-value"></a>Wartość zwracana

PRAWDA, jeśli ta metoda zakończy się pomyślnie; w przeciwnym razie FALSE.

### <a name="remarks"></a>Uwagi

Parametr *lpszEntry* to nazwa wpisu rejestru znajdującego się pod domyślnym kluczem rejestru dla aplikacji. Aby uzyskać lub ustawić domyślny klucz rejestru, należy użyć metod [CWinAppEx::GetRegistryBase](#getregistrybase) i [CWinAppEx::SetRegistryBase](#setregistrybase) odpowiednio.

Jeśli klucz określony przez *lspzEntry* nie istnieje, ta metoda utworzy go.

## <a name="see-also"></a>Zobacz też

[Wykres hierarchii](../../mfc/hierarchy-chart.md)<br/>
[Klasy](../../mfc/reference/mfc-classes.md)<br/>
[Klasa CWinApp](../../mfc/reference/cwinapp-class.md)<br/>
[Klasa CMouseManager](../../mfc/reference/cmousemanager-class.md)<br/>
[Klasa CContextMenuManager](../../mfc/reference/ccontextmenumanager-class.md)<br/>
[Klasa CKeyboardManager](../../mfc/reference/ckeyboardmanager-class.md)<br/>
[Klasa CUserToolsManager](../../mfc/reference/cusertoolsmanager-class.md)
